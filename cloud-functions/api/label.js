import {getAIConfig} from '../_shared/ai-config.js';
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=UTF-8','Cache-Control':'no-store'}});
const clean=(v,n=48)=>typeof v==='string'?v.replace(/[\r\n\t]+/g,' ').trim().slice(0,n):'';
function parse(v){const s=String(v||'').replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim();try{return clean(JSON.parse(s).label)}catch{}const a=s.indexOf('{'),b=s.lastIndexOf('}');if(a>=0&&b>a)try{return clean(JSON.parse(s.slice(a,b+1)).label)}catch{}return''}
export async function onRequestPost(context){const c=getAIConfig(context.env||{});if(!c.enabled||!c.insightEnabled)return json({available:false,error:'not_available'},503);let body={};try{body=await context.request.json()}catch{return json({error:'bad_json'},400)}const text=clean(body.text,40);if(!text)return json({error:'text'},400);if(/^[\x20-\x7e]+$/.test(text))return json({available:true,label:text});const prompt=`把这个健身器械、动作或训练项目名称翻译成健身房里自然、准确、简洁的英文标签。不要解释，不要逐字硬译；优先使用国际通用名称。只返回JSON：{"label":"英文标签"}。原文：${JSON.stringify(text)}`;const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),Math.min(8000,c.timeoutMs||8000));try{const r=await fetch(`${c.base}/chat/completions`,{method:'POST',signal:controller.signal,headers:{Authorization:`Bearer ${c.key}`,'Content-Type':'application/json'},body:JSON.stringify({model:c.insightModel,messages:[{role:'user',content:prompt}],temperature:0.05,max_tokens:80,enable_thinking:false,response_format:{type:'json_object'}})});const raw=await r.json();if(!r.ok)return json({available:true,error:'upstream'},502);const label=parse(raw?.choices?.[0]?.message?.content);return label?json({available:true,label}):json({available:true,error:'bad_response'},502)}catch(e){return json({available:true,error:e?.name==='AbortError'?'timeout':'network'},502)}finally{clearTimeout(timer)}}
export function onRequest(){return json({error:'method'},405)}
