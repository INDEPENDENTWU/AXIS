name: AXIS 8.21 Editor Hydration Diagnostic Once
on:
  push:
    branches: [fix/821-quick-photo-metric-centering]
    paths:
      - scripts/.axis-821-editor-hydration-diagnostic-once.mjs
      - .github/workflows/axis-821-editor-hydration-diagnostic-once.yml
permissions:
  contents: write
jobs:
  install:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/821-quick-photo-metric-centering
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - name: Install strict hydration diagnostics
        run: node scripts/.axis-821-editor-hydration-diagnostic-once.mjs
      - name: Validate unchanged strict smoke syntax
        run: node --check scripts/axis-821-recording-property-surface-smoke.mjs
      - name: Commit diagnostic and retire one-shot runner
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/axis-821-editor-hydration-diagnostic-once.yml scripts/.axis-821-editor-hydration-diagnostic-once.mjs
          git add scripts/axis-821-recording-property-surface-smoke.mjs
          git diff --cached --check
          git commit -m "test: deepen editor hydration diagnostics"
          git push origin HEAD:fix/821-quick-photo-metric-centering
