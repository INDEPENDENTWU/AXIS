# Current Work

## Production baseline at start of this work

AXIS **8.25.1 — Inline Set Morph** is the fully Production-sealed Web runtime inherited by this work.

- canonical repository: `INDEPENDENTWU/AXIS`
- exact sealed 8.25.1 `main`: `f4d3d02e1a7b655185806b2dbb3bface804d93cc`
- release PR: **#151**
- architecture: `canonical-single-runtime`
- canonical Vercel deployment: `dpl_DUuJiQoy8VnxRwcQLoGqCDckj1UX` — READY / Production / exact main SHA
- Current Release Gate `34761564240` — Chromium + iPhone-like WebKit success
- Deep Compatibility Gate `34761564210` — success
- EdgeOne Production run `34761564206` — exact-prebuilt deploy + parity + Chromium + WebKit success
- `axis.juele.fun` run `34761564467` — exact parity + Chromium + iPhone-like WebKit success
- fixed Vercel: `https://axis-five-puce.vercel.app`
- EdgeOne: `https://axisfitness-mirror-9x91gveo.edgeone.cool`
- governed custom domain: `https://axis.juele.fun`
- protected stores: `axis_v60_state`, `axis_v8_meta`, `axis_v89_speak`, `axis_v42_media`

**Chat history is not authoritative project memory.** Conversation history is supplemental only. Repository contracts, exact commits, built artifacts and provider certification are authoritative.

## Active change

**AXIS 8.26 — Active Continuity** is the current Release candidate on PR **#152** from the exact sealed 8.25.1 baseline.

- delivery branch: `release/826-active-continuity`
- governed target branch: `main`
- exact base: `f4d3d02e1a7b655185806b2dbb3bface804d93cc`
- release transition: **8.25.1 → 8.26**
- version decision: `bump`
- sequence: **13**
- change class: `product-ui`
- deterministic build graph: **88** top-level steps

This slice converges four user-visible issues without creating another truth owner:

1. **Atomic record-save presentation.** After a factual Encounter is committed, the recorder resets locally but Home no longer renders the short-lived committed-but-not-yet-Active intermediate screen. Existing v82 Active admission settles first, then one render presents the truthful next state. The old floating saving pill is retired; save feedback remains local to the button and the undo strip stays dock-safe.
2. **Executable Flow start.** `開始此項` now follows one explicit rule: ongoing modes (`sets`, `rounds`, `timed`, `hold`) enter the existing v82/v87 Active lifecycle immediately; one-shot modes use the canonical recorder because the record itself is their completion fact. A foreign Active item is paused/preserved through the existing switch coordination instead of being overwritten.
3. **Kinetic set cue.** The Production-sealed 8.25.1 inline fact-row morph remains factual authority. 8.26 adds one small, pointer-inert, non-layout `+1 組` kinetic cue at the safe edge of the existing Active stage after a real set completion. It has reduced-motion behavior and may not intersect the clock, controls, Capture dock or navigation.
4. **Quieter Home hierarchy.** Duplicate Active summary copy/compact metric blocks and redundant full-width separators are retired while the unique whole-session end control remains physically reachable. Hierarchy is carried by typography, spacing and bounded tonal grouping rather than repeated cards and rules.

No Session writer, Encounter writer, Active lifecycle owner, recorder, storage namespace/database, network owner, AI owner or historical rewrite is added.

## Inherited fail-closed governance provenance

- AXIS 8.25.1 sealed main: `f4d3d02e1a7b655185806b2dbb3bface804d93cc`
- AXIS 8.25 sealed main: `e28f0411288e42fc68e70a79a4180a06f7d18ee3`
- AXIS 8.24.1 sealed main: `d4b009c327f6fa9c4900eaa41d7195f10f6ed425`
- AXIS 8.24 sealed main: `321647b9aaca783b7f6ba99ec66616941208c698`
- AXIS 8.23 sealed main: `2418103c786f2d0865aece49d738e9ed9161ef55`
- native foundation: `axis-native-foundation-0` / `INDEPENDENTWU/AXIS-iOS`
- portable contracts retained: `axis.domain.v1`, `axis.data.v1`, `axis.flow.v1`, `axis.flow-provenance.v1`, `axis.report-range.v1`

## Validation for this work

Merge is blocked until one exact PR #152 head proves all of the following without weakening inherited assertions:

1. `node build-release.mjs` emits public/base **8.26**, `canonical-single-runtime`, one initial JavaScript request and zero dynamic runtime chunks in exactly **88 deterministic top-level steps**.
2. Version Authority proves **8.25.1 → 8.26 / bump / sequence 13 / product-ui** against the exact sealed 8.25.1 base.
3. Repository and Production governance explicitly model PR #152, the sealed 8.25.1 baseline, 88 steps and the 8.26 physical proof chain.
4. Chromium and iPhone-like WebKit prove record-save has no intermediate Home flash, an ongoing Flow item enters existing Active exactly once, one-shot Flow remains canonical recorder-owned, foreign Active is pause/preserved, and no extra Encounter/Active writer appears.
5. The kinetic set cue is post-fact, pointer-inert, reduced-motion-safe, causes no stage-height change or horizontal overflow, and never intersects clock, controls, Capture dock or bottom navigation.
6. Home simplification preserves all actionable information and the whole-session end control while removing duplicate summary/metric surfaces and redundant separator density.
7. All inherited Runtime, Current Release, Deep Compatibility, Repository, Work Continuity, Flow, Active, Session/Encounter, Object, Report, Backup and cross-platform gates settle green on the exact PR head.
8. After merge, the exact merged `main` SHA must be served by Vercel Production, mirrored as the exact prebuilt artifact to EdgeOne, and pass Chromium + WebKit parity/behavior proof on `axis.juele.fun`.

Failures are fixed at the actual owner. User data, ownership boundaries and meaningful physical assertions may not be weakened merely to make certification pass.

## Next planned stage

Do not begin another product or architecture slice until AXIS 8.26 is fully certified on the exact merged-main artifact through Vercel → EdgeOne → `axis.juele.fun`.
