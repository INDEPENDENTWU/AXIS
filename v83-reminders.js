(()=>{'use strict';
const D=document,$=(s,r=D)=>r.querySelector(s),$$=(s,r=D)=>Array.from(r.querySelectorAll(s));
const META='axis_v8_meta',CORE='axis_v60_state';
let audioCtx=null,audioPrimed=false,lastReminderKey='';
function readMeta(){try{const m=JSON.parse(localStorage.getItem(META)||'null')||{};m.events=m.events||{};m.prefs=m.prefs||{};return m}catch{return{events:{},prefs:{}}}}
function writeMeta(m){try{localStorage.setItem(META,JSON.stringify(m));return true}catch{return false}}
function readCore(){try{return JSON.parse(localStorage.getItem(CORE)||'null')||{active:null,sessions:[]}}catch{return{active:null,sessions:[]}}}
function ctx(){if(!audioCtx){const C=window.AudioContext||window.webkitAudioContext;if(!C)return null;audioCtx=new C()}return audioCtx}
function prime(){const c=ctx();if(!c||audioPrimed)return;try{const b=c.createBuffer(1,1,22050),s=c.createBufferSource(),g=c.createGain();g.gain.value=0;s.buffer=b;s.connect(g);g.connect(c.destination);s.start();audioPrimed=true}catch{}}
async function unlock(){const c=ctx();if(!c)return false;try{if(c.state!=='running')await c.resume();prime();return c.state==='running'}catch{return false}}
function tone(mode='soft'){const c=ctx();if(!c||c.state!=='running')return false;try{const t=c.currentTime+.015,g=c.createGain(),a=c.createOscillator(),b=c.createOscillator();a.type='sine';b.type='sine';a.frequency.value=mode==='clear'?720:620;b.frequency.value=mode==='clear'?1080:930;const peak=mode==='clear'?.055:.028;g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(peak,t+.025);g.gain.exponentialRampToValueAtTime(.0001,t+.22);a.connect(g);b.connect(g);g.connect(c.destination);a.start(t);b.start(t+.032);a.stop(t+.23);b.stop(t+.19);return true}catch{return false}}
function installUnlock(){const fn=async()=>{if((readMeta().prefs.reminderSound||'off')==='off')return;if(await unlock()){D.removeEventListener('pointerdown',fn,true);D.removeEventListener('touchend',fn,true);D.removeEventListener('keydown',fn,true)}};D.addEventListener('pointerdown',fn,true);D.addEventListener('touchend',fn,true);D.addEventListener('keydown',fn,true)}
function historicalRestMs(e){if(!e?.equipmentId)return 0;const c=readCore(),m=readMeta(),vals=[];const all=(c.active?[c.active]:[]).concat(c.sessions||[]).flatMap(s=>s.events||[]).filter(x=>x.equipmentId===e.equipmentId&&x.id!==e.id).sort((a,b)=>(b.time||0)-(a.time||0)).slice(0,8);for(const h of all){const times=(m.events?.[h.id]?.sets||[]).map(s=>Number(s.doneAt)||0).filter(Boolean).sort((a,b)=>a-b);for(let i=1;i<times.length;i++){const gap=times[i]-times[i-1]-45000;if(gap>=45000&&gap<=360000)vals.push(gap)}}if(vals.length<2)return 0;vals.sort((a,b)=>a-b);const i=Math.floor(vals.length/2),med=vals.length%2?vals[i]:(vals[i-1]+vals[i])/2;return Math.max(75000,Math.min(240000,Math.round(med/5000)*5000))}
function threshold(p,e){const v=String(p.reminderTiming||'auto');if(v==='90')return 90000;if(v==='120')return 120000;if(v==='180')return 180000;return historicalRestMs(e)||120000}
function activeRest(){const c=readCore(),m=readMeta();if(!c.active)return null;return(c.active.events||[]).map(e=>({e,a:m.events?.[e.id]?.activity})).filter(x=>x.a?.status==='active'&&x.a?.restStartedAt).sort((a,b)=>(b.a.restStartedAt||0)-(a.a.restStartedAt||0))[0]||null}
async function check(){const m=readMeta(),p=m.prefs||{},mode=p.reminderSound||'off';if(mode==='off'||D.visibilityState!=='visible')return;const x=activeRest();if(!x)return;const wait=threshold(p,x.e);if(Date.now()-x.a.restStartedAt<wait)return;const key=x.e.id+':'+x.a.restStartedAt;if(key===lastReminderKey||x.a.soundReminderFor===x.a.restStartedAt)return;if(!(await unlock()))return;tone(mode);lastReminderKey=key;const mm=readMeta();if(mm.events?.[x.e.id]?.activity){mm.events[x.e.id].activity.soundReminderFor=x.a.restStartedAt;writeMeta(mm)}}
function setSeg(host,val){if(!host)return;$$('button',host).forEach(b=>b.classList.toggle('active',b.dataset.v===String(val)))}
function inject(){const host=$('#settingsSheet .settingsList.second');if(!host||$('#v83Reminder'))return;const st=D.createElement('style');st.id='v83ReminderStyle';st.textContent='.v83Reminder{margin-top:1px}.v83Reminder .miniSeg{max-width:220px}.v83ReminderNote{padding:8px 0 2px;color:var(--dim);font-size:11.5px;line-height:1.55}.v83ReminderTest{height:36px;padding:0 12px;border-radius:11px;background:var(--s2);color:var(--accent2);font-size:11.5px;font-weight:650}.v83AudioStatus{font-size:11.5px!important;color:var(--muted)!important}';D.head.appendChild(st);const p=readMeta().prefs||{};host.insertAdjacentHTML('beforeend','<div id="v83Reminder" class="v83Reminder"><div class="settingPlain"><span>声音提醒</span><div class="miniSeg" id="v83Sound"><button data-v="off">关闭</button><button data-v="soft">轻声</button><button data-v="clear">清晰</button></div></div><div class="settingPlain"><span>提示时机</span><div class="miniSeg" id="v83Timing"><button data-v="auto">自动</button><button data-v="90">90秒</button><button data-v="120">2分</button><button data-v="180">3分</button></div></div><div class="settingPlain"><span>音频输出</span><b class="v83AudioStatus">跟随系统</b></div><div class="v83ReminderNote">耳机已连接时，提示音跟随 iPhone 当前音频输出。自动优先参考同一项目的历史组间节奏；每次休息最多提醒一次。</div><button class="v83ReminderTest" id="v83TestSound">试听提示音</button></div>');setSeg($('#v83Sound'),p.reminderSound||'off');setSeg($('#v83Timing'),p.reminderTiming||'auto');$('#v83Sound').onclick=async e=>{const b=e.target.closest('button');if(!b)return;const mm=readMeta();mm.prefs.reminderSound=b.dataset.v;writeMeta(mm);setSeg($('#v83Sound'),b.dataset.v);if(b.dataset.v!=='off'){await unlock();tone(b.dataset.v);installUnlock()}};$('#v83Timing').onclick=e=>{const b=e.target.closest('button');if(!b)return;const mm=readMeta();mm.prefs.reminderTiming=b.dataset.v;writeMeta(mm);setSeg($('#v83Timing'),b.dataset.v)};$('#v83TestSound').onclick=async()=>{const mode=readMeta().prefs.reminderSound||'soft';if(await unlock())tone(mode==='off'?'soft':mode)}}
function boot(){inject();installUnlock();setInterval(check,500);$('#settingsBtn')?.addEventListener('click',()=>setTimeout(inject,80));D.addEventListener('visibilitychange',()=>{if(!D.hidden){check();installUnlock()}});const v=$('.versionLine');if(v)v.textContent='版本 8.3.3'}
try{boot()}catch(e){console.warn('[AXIS] reminder enhancement skipped',e)}
})();