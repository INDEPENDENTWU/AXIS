import fs from 'node:fs';

const APP='app.js',HTML='index.html';
const fail=m=>{throw new Error(`[AXIS 8.21 portable backup] ${m}`)};
const once=(src,from,to,label)=>{const n=src.split(from).length-1;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(from,to)};
const code=s=>s.replace(/\\([`$])/g,'$1');

function findDeclarationEnd(src,start,label){
 let open=src.indexOf('{',start);if(open<0)fail(`${label} missing body`);
 let depth=0,quote='',line=false,block=false,escape=false;
 for(let i=open;i<src.length;i++){
  const c=src[i],n=src[i+1];
  if(line){if(c==='\n')line=false;continue}
  if(block){if(c==='*'&&n==='/'){block=false;i++}continue}
  if(quote){if(escape){escape=false;continue}if(c==='\\'){escape=true;continue}if(c===quote){quote='';continue}continue}
  if(c==='/'&&n==='/'){line=true;i++;continue}
  if(c==='/'&&n==='*'){block=true;i++;continue}
  if(c==='"'||c==="'"||c==='`'){quote=c;continue}
  if(c==='{')depth++;
  else if(c==='}'){depth--;if(depth===0)return i+1}
 }
 fail(`${label} unterminated body`)
}
function replaceFunctionDeclaration(src,name,to,label){
 const re=new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'),hits=[...src.matchAll(re)];
 if(hits.length!==1)fail(`${label} expected one function owner, found ${hits.length}`);
 const start=hits[0].index,end=findDeclarationEnd(src,start,label);
 return src.slice(0,start)+to+src.slice(end)
}
function replaceObjectAssignment(src,prefix,to,label){
 const hits=[];for(let p=src.indexOf(prefix);p>=0;p=src.indexOf(prefix,p+prefix.length))hits.push(p);
 if(hits.length!==1)fail(`${label} expected one owner statement, found ${hits.length}`);
 const start=hits[0],bodyEnd=findDeclarationEnd(src,start,label);let end=bodyEnd;
 while(end<src.length&&/\s/.test(src[end]))end++;
 if(src[end]!==';')fail(`${label} missing object-assignment terminator`);
 return src.slice(0,start)+to+src.slice(end+1)
}
function findElementById(src,id,label){
 const marker=`id="${id}"`,hits=[];for(let p=src.indexOf(marker);p>=0;p=src.indexOf(marker,p+marker.length))hits.push(p);
 if(hits.length!==1)fail(`${label} expected one element, found ${hits.length}`);
 const at=hits[0],start=src.lastIndexOf('<',at),openEnd=src.indexOf('>',at);
 if(start<0||openEnd<0)fail(`${label} opening boundary missing`);
 const match=/^<([A-Za-z][\w:-]*)\b/.exec(src.slice(start,openEnd+1));if(!match)fail(`${label} tag name missing`);
 const tag=match[1],closeToken=`</${tag}>`,close=src.indexOf(closeToken,openEnd);
 if(close<0)fail(`${label} closing boundary missing`);
 return{start,end:close+closeToken.length,tag}
}
function replaceElementById(src,id,to,label){const b=findElementById(src,id,label);return src.slice(0,b.start)+to+src.slice(b.end)}
function insertAfterElementById(src,id,to,label){const b=findElementById(src,id,label);return src.slice(0,b.end)+to+src.slice(b.end)}

let app=fs.readFileSync(APP,'utf8');
let html=fs.readFileSync(HTML,'utf8');

/* Portable backup extends the one canonical app-owned media bridge. User export
 * never bulk-reads the store: keys are enumerated first and values are fetched one
 * at a time. Restore encodes and commits one media object at a time as well. */
const mediaBridgeNext=String.raw`async function axisBackupMediaKeys(){
 const db=await openDb();return new Promise((res,rej)=>{let tx=null,req=null,settled=false;const bad=e=>{if(settled)return;settled=true;try{db.close()}catch{}rej(tx?.error||req?.error||e?.target?.error||e||new Error('media-backup-keys-failed'))};try{tx=db.transaction('media','readonly');req=tx.objectStore('media').getAllKeys();req.onsuccess=()=>{if(settled)return;settled=true;const out=(req.result||[]).map(String).sort((a,b)=>a.localeCompare(b));db.close();res(out)};req.onerror=bad;tx.onabort=bad;tx.onerror=()=>{}}catch(e){bad(e)}})
}
async function axisBackupMediaEntries(){
 const keys=await axisBackupMediaKeys(),out=[];for(const key of keys){const blob=await getMedia(key);if(!(blob instanceof Blob))throw new Error('canonical-media-entry-invalid');out.push({key,blob})}return out
}
async function axisBackupMediaReplaceAll(entries){
 const rows=(entries||[]).map(item=>{if(!item||typeof item.key!=='string'||!(item.blob instanceof Blob))throw new Error('media-backup-entry-invalid');return{key:item.key,blob:item.blob}}),db=await openDb();
 const txRun=fn=>new Promise((res,rej)=>{let tx=null,settled=false;const bad=e=>{if(settled)return;settled=true;rej(tx?.error||e?.target?.error||e||new Error('media-backup-write-failed'))};try{tx=db.transaction('media','readwrite');fn(tx.objectStore('media'));tx.oncomplete=()=>{if(settled)return;settled=true;res()};tx.onabort=bad;tx.onerror=()=>{}}catch(e){bad(e)}});
 try{await txRun(store=>store.clear());for(const item of rows){const value=await mediaEncodeValue(item.blob);await txRun(store=>store.put(value,item.key))}}finally{try{db.close()}catch{}}
}
window.__AXIS_MEDIA_STORE__={get:getMedia,put:putMedia,del:deleteMedia,format:AXIS_MEDIA_FORMAT,entries:axisBackupMediaEntries,replaceAll:axisBackupMediaReplaceAll,keys:axisBackupMediaKeys};`;
app=replaceObjectAssignment(app,'window.__AXIS_MEDIA_STORE__=',mediaBridgeNext,'canonical media bridge extension');

/* v1 remains an import/compatibility contract. v2 is the sole user-facing export
 * format: a small sealed JSON header followed by raw media Blob parts. */
const newBackup=code(String.raw`const AXIS_BACKUP_SCHEMA='axis.backup.v1',AXIS_BACKUP_MIME='application/vnd.axis.backup+json',AXIS_BACKUP_PREFIX='axis_';
const AXIS_BACKUP_V2_SCHEMA='axis.backup.v2',AXIS_BACKUP_V2_MAGIC='AXISB2\n',AXIS_BACKUP_V2_MIME='application/vnd.axis.backup';
const axisBackupUtf8Bytes=s=>new TextEncoder().encode(String(s)).byteLength;
function axisBackupCanonical(v){if(Array.isArray(v))return '['+v.map(axisBackupCanonical).join(',')+']';if(v&&typeof v==='object'){return '{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+axisBackupCanonical(v[k])).join(',')+'}'}return JSON.stringify(v)}
async function axisBackupSha256(v){const b=new TextEncoder().encode(typeof v==='string'?v:axisBackupCanonical(v)),h=await crypto.subtle.digest('SHA-256',b);return Array.from(new Uint8Array(h),x=>x.toString(16).padStart(2,'0')).join('')}
function axisBackupStorageSnapshot(){const out=[];for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);if(key&&key.startsWith(AXIS_BACKUP_PREFIX))out.push({key,value:localStorage.getItem(key)??''})}return out.sort((a,b)=>a.key.localeCompare(b.key))}
function axisBackupStorageReplace(entries){const next=new Map((entries||[]).map(x=>[String(x.key),String(x.value)]));for(const row of axisBackupStorageSnapshot())if(!next.has(row.key))localStorage.removeItem(row.key);for(const [key,value] of next)localStorage.setItem(key,value)}
function axisBackupB64FromBuffer(buf){const u=new Uint8Array(buf),step=0x8000;let s='';for(let i=0;i<u.length;i+=step)s+=String.fromCharCode(...u.subarray(i,Math.min(i+step,u.length)));return btoa(s)}
function axisBackupBlobFromB64(b64,type){const raw=atob(String(b64||'')),step=0x8000,parts=[];for(let i=0;i<raw.length;i+=step){const end=Math.min(i+step,raw.length),u=new Uint8Array(end-i);for(let j=i;j<end;j++)u[j-i]=raw.charCodeAt(j);parts.push(u)}return new Blob(parts,{type:type||'application/octet-stream'})}
async function axisBackupMediaSnapshot(){const store=window.__AXIS_MEDIA_STORE__;if(!store?.entries)throw new Error('canonical-media-backup-unavailable');const rows=await store.entries(),out=[];for(const row of rows){if(!row||typeof row.key!=='string'||!(row.blob instanceof Blob))throw new Error('canonical-media-entry-invalid');const bytes=await row.blob.arrayBuffer();out.push({key:String(row.key),type:row.blob.type||'application/octet-stream',size:bytes.byteLength,encoding:'base64',data:axisBackupB64FromBuffer(bytes)})}return out.sort((a,b)=>a.key.localeCompare(b.key))}
async function axisBackupMediaDecode(entries){const out=[];for(const row of entries||[]){if(!row||typeof row.key!=='string'||row.encoding!=='base64'||typeof row.data!=='string')throw new Error('backup-media-invalid');const blob=axisBackupBlobFromB64(row.data,row.type);if(Number(row.size)!==blob.size)throw new Error('backup-media-size-mismatch');out.push({key:row.key,blob})}return out}
function axisBackupSummary(storage,media){let sessions=0,events=0;const core=storage.find(x=>x.key==='axis_v60_state');if(core){try{const c=JSON.parse(core.value);sessions=Array.isArray(c.sessions)?c.sessions.length:0;events=(c.sessions||[]).reduce((n,s)=>n+(Array.isArray(s?.events)?s.events.length:0),0)+(Array.isArray(c.active?.events)?c.active.events.length:0)}catch{}}return{storageCount:storage.length,storageBytes:storage.reduce((n,x)=>n+axisBackupUtf8Bytes(x.key)+axisBackupUtf8Bytes(x.value),0),mediaCount:media.length,mediaBytes:media.reduce((n,x)=>n+(Number(x.size)||0),0),sessions,events}}
function axisBackupSource(){return{platform:'web',appVersion:String(window.__AXIS_VERSION__||VERSION||'8.21'),domain:'axis.domain.v1',data:'axis.data.v1',exchange:'axis.exchange.v1',gitSha:String(window.__AXIS_GIT_SHA__||document.documentElement.dataset.axisGitSha||'unknown'),origin:location.origin}}
async function axisBackupCreateBundle(){save();const storage=axisBackupStorageSnapshot(),media=await axisBackupMediaSnapshot(),manifest=axisBackupSummary(storage,media),payload={schema:AXIS_BACKUP_SCHEMA,exportedAt:new Date().toISOString(),source:axisBackupSource(),webOriginSnapshot:{storage,media},manifest};return{...payload,integrity:{algorithm:'SHA-256',value:await axisBackupSha256(payload)}}}
async function axisBackupVerifyBundle(bundle){if(!bundle||bundle.schema!==AXIS_BACKUP_SCHEMA)throw new Error('backup-schema-unsupported');if(bundle.integrity?.algorithm!=='SHA-256'||typeof bundle.integrity?.value!=='string')throw new Error('backup-integrity-missing');const payload={schema:bundle.schema,exportedAt:bundle.exportedAt,source:bundle.source,webOriginSnapshot:bundle.webOriginSnapshot,manifest:bundle.manifest},actual=await axisBackupSha256(payload);if(actual!==bundle.integrity.value)throw new Error('backup-integrity-mismatch');const storage=bundle.webOriginSnapshot?.storage,media=bundle.webOriginSnapshot?.media;if(!Array.isArray(storage)||!Array.isArray(media))throw new Error('backup-snapshot-invalid');const storageKeys=new Set(),mediaKeys=new Set();for(const row of storage){if(!row||typeof row.key!=='string'||!row.key.startsWith(AXIS_BACKUP_PREFIX)||typeof row.value!=='string'||storageKeys.has(row.key))throw new Error('backup-storage-invalid');storageKeys.add(row.key)}for(const row of media){if(!row||typeof row.key!=='string'||typeof row.type!=='string'||row.encoding!=='base64'||typeof row.data!=='string'||!Number.isFinite(Number(row.size))||Number(row.size)<0||mediaKeys.has(row.key))throw new Error('backup-media-invalid');mediaKeys.add(row.key)}const summary=axisBackupSummary(storage,media);for(const k of ['storageCount','storageBytes','mediaCount','mediaBytes'])if(Number(bundle.manifest?.[k])!==Number(summary[k]))throw new Error('backup-manifest-mismatch');return{payload,storage,media,summary,format:'v1'}}
async function axisBackupVerifyCurrent(storage,media){const nowStorage=axisBackupStorageSnapshot();if(axisBackupCanonical(nowStorage)!==axisBackupCanonical(storage))throw new Error('restore-storage-verify-failed');const nowMedia=await axisBackupMediaSnapshot();const compact=x=>(x||[]).map(r=>({key:r.key,type:r.type,size:r.size,encoding:r.encoding,data:r.data}));if(axisBackupCanonical(compact(nowMedia))!==axisBackupCanonical(compact(media)))throw new Error('restore-media-verify-failed')}
function axisBackupV2Hex(buf){return Array.from(new Uint8Array(buf),x=>x.toString(16).padStart(2,'0')).join('')}
async function axisBackupV2BlobSha(blob){return axisBackupV2Hex(await crypto.subtle.digest('SHA-256',await blob.arrayBuffer()))}
function axisBackupV2Summary(storage,media){return axisBackupSummary(storage,media)}
async function axisBackupV2BuildFile(){save();const storage=axisBackupStorageSnapshot(),store=window.__AXIS_MEDIA_STORE__;if(!store?.keys||!store?.get)throw new Error('canonical-media-v2-unavailable');const keys=await store.keys(),media=[],mediaParts=[];let offset=0;for(const key of keys){const blob=await store.get(key);if(!(blob instanceof Blob))throw new Error('canonical-media-entry-invalid');const size=blob.size,type=blob.type||'application/octet-stream',sha256=await axisBackupV2BlobSha(blob);media.push({key:String(key),type,size,offset,sha256});mediaParts.push(blob);offset+=size}const manifest=axisBackupV2Summary(storage,media),payload={schema:AXIS_BACKUP_V2_SCHEMA,exportedAt:new Date().toISOString(),source:axisBackupSource(),webOriginSnapshot:{storage,media},manifest},header={...payload,integrity:{algorithm:'SHA-256',value:await axisBackupSha256(payload)}},headerBytes=new TextEncoder().encode(JSON.stringify(header)),magicBytes=new TextEncoder().encode(AXIS_BACKUP_V2_MAGIC),lenBytes=new Uint8Array(4);new DataView(lenBytes.buffer).setUint32(0,headerBytes.length,false);const file=new File([magicBytes,lenBytes,headerBytes,...mediaParts],'AXIS-'+new Date().toISOString().slice(0,10)+'.axisbackup',{type:AXIS_BACKUP_V2_MIME});return{file,header}}
async function axisBackupV2ReadHeader(file){const magicBytes=new TextEncoder().encode(AXIS_BACKUP_V2_MAGIC),prefixSize=magicBytes.length+4;if(!(file instanceof Blob)||file.size<prefixSize)throw new Error('backup-v2-too-small');const prefix=new Uint8Array(await file.slice(0,prefixSize).arrayBuffer());for(let i=0;i<magicBytes.length;i++)if(prefix[i]!==magicBytes[i])throw new Error('backup-v2-magic-mismatch');const headerLength=new DataView(prefix.buffer,prefix.byteOffset+magicBytes.length,4).getUint32(0,false);if(headerLength<2||headerLength>16*1024*1024||prefixSize+headerLength>file.size)throw new Error('backup-v2-header-invalid');const headerText=await file.slice(prefixSize,prefixSize+headerLength).text(),header=JSON.parse(headerText);return{header,mediaStart:prefixSize+headerLength}}
async function axisBackupV2VerifyFile(file){const {header,mediaStart}=await axisBackupV2ReadHeader(file);if(header?.schema!==AXIS_BACKUP_V2_SCHEMA)throw new Error('backup-schema-unsupported');if(header.integrity?.algorithm!=='SHA-256'||typeof header.integrity?.value!=='string')throw new Error('backup-integrity-missing');const payload={schema:header.schema,exportedAt:header.exportedAt,source:header.source,webOriginSnapshot:header.webOriginSnapshot,manifest:header.manifest};if(await axisBackupSha256(payload)!==header.integrity.value)throw new Error('backup-integrity-mismatch');const storage=header.webOriginSnapshot?.storage,media=header.webOriginSnapshot?.media;if(!Array.isArray(storage)||!Array.isArray(media))throw new Error('backup-snapshot-invalid');const storageKeys=new Set(),mediaKeys=new Set();for(const row of storage){if(!row||typeof row.key!=='string'||!row.key.startsWith(AXIS_BACKUP_PREFIX)||typeof row.value!=='string'||storageKeys.has(row.key))throw new Error('backup-storage-invalid');storageKeys.add(row.key)}let offset=0;for(const row of media){if(!row||typeof row.key!=='string'||typeof row.type!=='string'||!Number.isFinite(Number(row.size))||Number(row.size)<0||Number(row.offset)!==offset||typeof row.sha256!=='string'||!/^[0-9a-f]{64}$/.test(row.sha256)||mediaKeys.has(row.key))throw new Error('backup-media-invalid');mediaKeys.add(row.key);offset+=Number(row.size)}if(mediaStart+offset!==file.size)throw new Error('backup-media-size-mismatch');const summary=axisBackupV2Summary(storage,media);for(const k of ['storageCount','storageBytes','mediaCount','mediaBytes'])if(Number(header.manifest?.[k])!==Number(summary[k]))throw new Error('backup-manifest-mismatch');for(const row of media){const blob=file.slice(mediaStart+row.offset,mediaStart+row.offset+row.size,row.type);if(await axisBackupV2BlobSha(blob)!==row.sha256)throw new Error('backup-media-integrity-mismatch')}return{bundle:header,verified:{format:'v2',storage,media,summary,file,mediaStart}}}
async function axisBackupV2CurrentRaw(){const store=window.__AXIS_MEDIA_STORE__;if(!store?.keys||!store?.get)throw new Error('canonical-media-v2-unavailable');const keys=await store.keys(),out=[];for(const key of keys){const blob=await store.get(key);if(!(blob instanceof Blob))throw new Error('canonical-media-entry-invalid');out.push({key:String(key),blob,type:blob.type||'application/octet-stream',size:blob.size,sha256:await axisBackupV2BlobSha(blob)})}return out}
async function axisBackupV2VerifyCurrent(storage,media){if(axisBackupCanonical(axisBackupStorageSnapshot())!==axisBackupCanonical(storage))throw new Error('restore-storage-verify-failed');const store=window.__AXIS_MEDIA_STORE__,keys=await store.keys();if(keys.length!==media.length)throw new Error('restore-media-verify-failed');for(let i=0;i<media.length;i++){const row=media[i];if(keys[i]!==row.key)throw new Error('restore-media-verify-failed');const blob=await store.get(row.key);if(!(blob instanceof Blob)||blob.size!==Number(row.size)||(blob.type||'application/octet-stream')!==row.type||await axisBackupV2BlobSha(blob)!==row.sha256)throw new Error('restore-media-verify-failed')}}
async function axisBackupV2DeliverReady(){const ready=window.__AXIS_BACKUP_EXPORT_READY__,btn=$('#backupBtn');if(!ready?.file)return false;const file=ready.file;if(navigator.canShare?.({files:[file]})&&navigator.share){try{await navigator.share({files:[file],title:'AXIS 完整备份'});window.__AXIS_BACKUP_EXPORT_READY__=null;if(btn){btn.textContent='建立完整 AXIS 备份';btn.disabled=false}toast('完整备份已保存');return true}catch(e){if(e?.name==='AbortError'){toast('备份仍已准备好 · 可再次保存');return true}console.warn('[AXIS backup share]',e)}}const url=URL.createObjectURL(file),a=D.createElement('a');a.href=url;a.download=file.name||'AXIS.axisbackup';D.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),4000);window.__AXIS_BACKUP_EXPORT_READY__=null;if(btn){btn.textContent='建立完整 AXIS 备份';btn.disabled=false}toast('完整备份已保存');return true}
async function backupData(){const btn=$('#backupBtn');if(await axisBackupV2DeliverReady())return;if(window.__AXIS_BACKUP_EXPORT_BUSY__)return;window.__AXIS_BACKUP_EXPORT_BUSY__=true;if(btn){btn.disabled=true;btn.textContent='正在准备备份…'}try{toast('正在准备完整备份 · 不会上传');const ready=await axisBackupV2BuildFile();window.__AXIS_BACKUP_EXPORT_READY__=ready;if(btn){btn.disabled=false;btn.textContent='保存完整备份'}toast('备份已准备好 · 再点一次保存')}catch(e){console.error('[AXIS backup v2]',e);window.__AXIS_BACKUP_EXPORT_READY__=null;if(btn){btn.disabled=false;btn.textContent='建立完整 AXIS 备份'}toast('备份失败 · 本机数据未改变')}finally{window.__AXIS_BACKUP_EXPORT_BUSY__=false}}
function axisBackupRestoreSheet(){return $('#backupRestoreSheet')}
function axisBackupFormatBytes(n){return fmtBytes(Math.max(0,Number(n)||0))}
function axisBackupRenderPreview(bundle,verified){const m=verified.summary,s=bundle.source||{};setText('#backupRestoreSource',(s.appVersion||'AXIS')+' · '+(s.origin||'未知来源'));setText('#backupRestoreIntegrity','完整性验证通过');setText('#backupRestoreSessions',String(m.sessions));setText('#backupRestoreEvents',String(m.events));setText('#backupRestoreStorage',String(m.storageCount));setText('#backupRestoreMedia',String(m.mediaCount)+' · '+axisBackupFormatBytes(m.mediaBytes));const btn=$('#backupRestoreConfirm');if(btn){btn.disabled=false;btn.dataset.ready='1'}axisBackupRestoreSheet()?openSheet('backupRestoreSheet'):null}
async function axisBackupPickRestore(){if(state.active)return toast('请先结束当前训练再恢复备份');let input=$('#axisBackupFileInput');if(!input){input=D.createElement('input');input.id='axisBackupFileInput';input.type='file';input.hidden=true;input.accept='.axisbackup,'+AXIS_BACKUP_MIME+','+AXIS_BACKUP_V2_MIME+',application/json';D.body.appendChild(input);input.addEventListener('change',async()=>{const file=input.files?.[0];input.value='';if(!file)return;try{let bundle,verified;const magic=await file.slice(0,new TextEncoder().encode(AXIS_BACKUP_V2_MAGIC).length).text();if(magic===AXIS_BACKUP_V2_MAGIC){const parsed=await axisBackupV2VerifyFile(file);bundle=parsed.bundle;verified=parsed.verified}else{bundle=JSON.parse(await file.text());verified=await axisBackupVerifyBundle(bundle)}window.__AXIS_BACKUP_PENDING__={bundle,verified};axisBackupRenderPreview(bundle,verified)}catch(e){console.warn('[AXIS backup import rejected]',e);window.__AXIS_BACKUP_PENDING__=null;toast('无法恢复 · 备份文件未通过完整性验证')}})}input.click()}
async function axisBackupRestorePending(){if(state.active)return toast('请先结束当前训练再恢复备份');const pending=window.__AXIS_BACKUP_PENDING__;if(!pending?.bundle)return toast('请重新选择备份文件');const store=window.__AXIS_MEDIA_STORE__;if(!store?.replaceAll)return toast('恢复能力尚未就绪');const {storage,media}=pending.verified,beforeStorage=axisBackupStorageSnapshot(),beforeMedia=await axisBackupV2CurrentRaw(),btn=$('#backupRestoreConfirm');if(btn){btn.disabled=true;btn.textContent='正在恢复…'}try{const incoming=pending.verified.format==='v2'?media.map(row=>({key:row.key,blob:pending.verified.file.slice(pending.verified.mediaStart+row.offset,pending.verified.mediaStart+row.offset+row.size,row.type)})):await axisBackupMediaDecode(media);await store.replaceAll(incoming);axisBackupStorageReplace(storage);if(pending.verified.format==='v2')await axisBackupV2VerifyCurrent(storage,media);else await axisBackupVerifyCurrent(storage,media);window.__AXIS_BACKUP_PENDING__=null;setText('#backupRestoreIntegrity','完整恢复 · '+storage.length+'/'+storage.length+' 数据 · '+media.length+'/'+media.length+' 媒体');if(btn)btn.textContent='恢复完成';toast('完整恢复已验证');setTimeout(()=>location.reload(),850)}catch(e){console.error('[AXIS restore]',e);let rollbackError=null;try{await store.replaceAll(beforeMedia.map(row=>({key:row.key,blob:row.blob})));axisBackupStorageReplace(beforeStorage);await axisBackupV2VerifyCurrent(beforeStorage,beforeMedia)}catch(r){rollbackError=r;console.error('[AXIS restore rollback]',r)}if(btn){btn.disabled=false;btn.textContent='确认恢复'}setText('#backupRestoreIntegrity',rollbackError?'恢复失败 · 回滚校验异常':'恢复失败 · 已完整回滚');toast(rollbackError?'恢复失败 · 请保留当前页面':'恢复失败 · 原数据已恢复')}}
window.__AXIS_PORTABLE_BACKUP__={schema:AXIS_BACKUP_SCHEMA,exportSchema:AXIS_BACKUP_V2_SCHEMA,storagePrefix:AXIS_BACKUP_PREFIX,owner:'transport-only',network:false,create:axisBackupCreateBundle,verify:axisBackupVerifyBundle,createFile:axisBackupV2BuildFile,verifyFile:axisBackupV2VerifyFile,restore:async bundle=>{if(state.active)throw new Error('restore-active-session-blocked');const verified=await axisBackupVerifyBundle(bundle);window.__AXIS_BACKUP_PENDING__={bundle,verified};return axisBackupRestorePending()},restoreFile:async file=>{if(state.active)throw new Error('restore-active-session-blocked');const parsed=await axisBackupV2VerifyFile(file);window.__AXIS_BACKUP_PENDING__=parsed;return axisBackupRestorePending()}};`);
app=replaceFunctionDeclaration(app,'backupData',newBackup,'portable backup source owner');

const backupBinding="$('#backupBtn').onclick=backupData;";
app=once(app,backupBinding,backupBinding+"$('#restoreBackupBtn').onclick=axisBackupPickRestore;$('#backupRestoreConfirm').onclick=axisBackupRestorePending;",'backup/restore bindings');

const backupActions='<button id="backupBtn">建立完整 AXIS 备份</button><button id="restoreBackupBtn">从 AXIS 备份恢复</button>';
html=replaceElementById(html,'backupBtn',backupActions,'storage backup actions');
html=insertAfterElementById(html,'restoreBackupBtn','<div class="axisBackupNote">备份包含 AXIS 本机数据与媒体，可用于迁移到另一 AXIS 网域。</div>','storage backup note');

const restoreSheet=String.raw`<div class="sheetWrap" id="backupRestoreSheet"><div class="sheet axisBackupRestoreSheet">
  <div class="grabber"></div><div class="sheetHead"><b>恢复 AXIS 备份</b><button class="closeBtn" data-close="backupRestoreSheet">×</button></div>
  <div class="axisBackupVerify"><span id="backupRestoreIntegrity">等待验证</span><b id="backupRestoreSource">—</b></div>
  <div class="axisBackupStats"><div><span>训练</span><b id="backupRestoreSessions">0</b></div><div><span>记录</span><b id="backupRestoreEvents">0</b></div><div><span>数据项</span><b id="backupRestoreStorage">0</b></div><div><span>媒体</span><b id="backupRestoreMedia">0</b></div></div>
  <p class="axisBackupWarning">恢复会用这份备份替换此 AXIS 网域的本机数据。开始前会先建立本机回滚快照；只有数据与媒体逐项验证一致后才会完成。</p>
  <button class="primary" id="backupRestoreConfirm" disabled>确认恢复</button>
</div></div>`;
html=once(html,'</body>',restoreSheet+'\n</body>','restore sheet');

const backupStyle=String.raw`<style id="axis821PortableBackupStyle">
#storageSheet .storageActions{grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}#storageSheet #backupBtn,#storageSheet #restoreBackupBtn,#storageSheet #clearVideos{grid-column:1/-1;min-height:46px}#storageSheet #backupBtn{grid-column:1/-1;background:var(--accent);color:#fff}#storageSheet #restoreBackupBtn{grid-column:1/-1;background:#151820}.axisBackupNote{margin:9px 2px 2px;color:var(--dim);font-size:10px;line-height:1.55}.axisBackupRestoreSheet{padding-bottom:calc(22px + env(safe-area-inset-bottom))}.axisBackupVerify{padding:14px 0;border-bottom:1px solid var(--line2)}.axisBackupVerify span{display:block;color:#aeb3ff;font-size:11px;font-weight:650}.axisBackupVerify b{display:block;margin-top:5px;color:var(--muted);font-size:10px;font-weight:550;overflow-wrap:anywhere}.axisBackupStats{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1px;margin:14px 0;background:var(--line2)}.axisBackupStats div{min-height:70px;padding:12px;background:var(--bg2);display:flex;flex-direction:column;justify-content:space-between}.axisBackupStats span{color:var(--dim);font-size:10px}.axisBackupStats b{font-size:15px;font-variant-numeric:tabular-nums}.axisBackupWarning{margin:0 0 14px;color:var(--muted);font-size:10.5px;line-height:1.65}.axisBackupRestoreSheet #backupRestoreConfirm{width:100%;min-height:52px;border-radius:14px}.axisBackupRestoreSheet #backupRestoreConfirm:disabled{opacity:.45}
@media(max-width:390px){.axisBackupStats div{min-height:66px}.axisBackupRestoreSheet #backupRestoreConfirm{min-height:50px}}
</style>`;
html=once(html,'</head>',backupStyle+'\n</head>','backup style');

try{new Function(app)}catch(e){fail(`app syntax ${e.message}`)}
fs.writeFileSync(APP,app);fs.writeFileSync(HTML,html);
console.log('[AXIS 8.21 portable backup] PASS · one canonical source owner · preferred v2 raw-blob binary export · v1 import/API compatibility · sequential media transport · SHA-256 integrity · staged restore + verified rollback · no network owner');