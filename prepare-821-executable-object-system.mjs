import fs from 'node:fs';

const fail=m=>{throw new Error(`[AXIS 8.21 Executable Object System] ${m}`)};
const read=f=>{if(!fs.existsSync(f))fail(`missing ${f}`);return fs.readFileSync(f,'utf8')};
const write=(f,s)=>fs.writeFileSync(f,s);
const once=(src,from,to,label)=>{const n=src.split(from).length-1;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(from,to)};
function functionRange(src,signature,label){
 const start=src.indexOf(signature);if(start<0)fail(`${label} signature missing`);if(src.indexOf(signature,start+signature.length)>=0)fail(`${label} duplicated`);
 const brace=src.indexOf('{',start+signature.length-1);if(brace<0)fail(`${label} brace missing`);let depth=0,quote='',escaped=false,line=false,block=false,end=-1;
 for(let i=brace;i<src.length;i++){
  const ch=src[i],next=src[i+1]||'';
  if(line){if(ch==='\n')line=false;continue}
  if(block){if(ch==='*'&&next==='/'){block=false;i++}continue}
  if(quote){if(escaped){escaped=false;continue}if(ch==='\\'){escaped=true;continue}if(ch===quote)quote='';continue}
  if(ch==='/'&&next==='/'){line=true;i++;continue}
  if(ch==='/'&&next==='*'){block=true;i++;continue}
  if(ch==='"'||ch==="'"||ch==='`'){quote=ch;continue}
  if(ch==='{')depth++;else if(ch==='}'){depth--;if(depth===0){end=i+1;break}}
 }
 if(end<0)fail(`${label} closing brace missing`);return{start,end,text:src.slice(start,end)};
}
function replaceFunction(src,signature,replacement,label){const r=functionRange(src,signature,label);return src.slice(0,r.start)+replacement+src.slice(r.end)}
const syntax=(s,f)=>{try{new Function(s)}catch(e){fail(`${f} syntax ${e.message}`)}};

/*
 * One executable Object contract spans definition -> recorder -> immutable
 * Encounter -> existing Active -> history. This pass adds no state namespace,
 * database, recorder, picker, Encounter writer or Active owner.
 */

/* app.js — execution semantics and schema-aware factual presentation. */
{
 const FILE='app.js';let s=read(FILE);
 const execHelpers=String.raw`
const AXIS821_EXECUTION_MODES=new Set(['single','sets','rounds','timed','hold','complete']);
const AXIS821_CONTINUOUS_METRICS=new Set(['duration','distance','pace','speed','intensity','resistance','level','incline']);
function axis821ExecutionExplicit(eq){const v=String(eq?.executionMode||eq?.recording?.executionMode||'').trim();return AXIS821_EXECUTION_MODES.has(v)?v:''}
function axis821AutoExecutionMode(eq,schema=axis818SchemaForEq(eq)){
 const explicit=axis821ExecutionExplicit(eq);if(explicit)return explicit;
 const keys=new Set((schema||[]).map(x=>String(x?.key||x?.id||'')).filter(Boolean));
 if(keys.has('hold'))return'hold';
 if(keys.has('sets')||keys.has('rounds')||(keys.has('weight')&&keys.has('reps')))return'sets';
 if(keys.has('completed')&&keys.size===1)return'complete';
 if([...keys].some(k=>AXIS821_CONTINUOUS_METRICS.has(k)))return'timed';
 return'single'
}
function axis821ExecutionLabel(mode){return({sets:'分组进行',rounds:'循环轮次',timed:'连续计时',hold:'保持计时',single:'一次记录',complete:'完成即记'}[String(mode||'')]||'一次记录')}
`;
 const execAnchor='function axis820ExecutionModeForEq(ref)';
 if(!s.includes(execAnchor))fail('execution resolver anchor missing');
 s=s.replace(execAnchor,execHelpers+execAnchor);
 s=replaceFunction(s,'function axis820ExecutionModeForEq(ref)',`function axis820ExecutionModeForEq(ref){const eq=axis818Eq(ref);if(!eq)return'single';return axis821AutoExecutionMode(eq,axis818SchemaForEq(eq))}`,'Object execution resolver');
 s=replaceFunction(s,'function axis821ExecutionForRecording(ref)',`function axis821ExecutionForRecording(ref){const step=axis821CurrentForObject(ref),eq=axis818Eq(ref);if(!eq)return'single';if(step?.executionOverride&&AXIS821_FLOW_MODES.has(step.executionOverride))return step.executionOverride;const override=axis821OverrideSchema(step);if(override?.length&&!axis821ExecutionExplicit(eq))return axis821AutoExecutionMode(eq,override);return axis821AutoExecutionMode(eq,axis821SchemaForRecording(eq))}`,'Flow recording execution resolver');

 const historyHelpers=String.raw`
function axis821MetricValueForEvent(e,m){const k=String(m?.key||m?.id||'');if(!k)return null;const vals=e?.metrics&&typeof e.metrics==='object'?e.metrics:{};if(Object.prototype.hasOwnProperty.call(vals,k))return vals[k];if(Object.prototype.hasOwnProperty.call(e||{},k))return e[k];return null}
function axis821MetricValueText(m,v){if(v==null||v===''||String(v)==='undefined'||String(v)==='NaN')return'';const type=String(m?.type||''),unit=String(m?.unit||'');if(type==='boolean')return(v===true||v===1||v==='1'||v==='true')?'是':'否';if(String(m?.key)==='pace'&&typeof v==='string')return v+(unit?' '+unit:'');const n=Number(v),base=Number.isFinite(n)?(n%1?String(Math.round(n*100)/100):String(n)):String(v);return base+(unit?' '+unit:'')}
function axis821EventMetricEntries(e){
 let schema=Array.isArray(e?.metricSchemaSnapshot)?e.metricSchemaSnapshot:null;
 if(schema===null){const legacy=[];if(e?.kind==='strength'){if(e?.weight!=null)legacy.push({key:'weight',label:'重量',unit:'kg',type:'number'});if(e?.reps!=null)legacy.push({key:'reps',label:'次数',unit:'次',type:'count'});if(e?.sets!=null)legacy.push({key:'sets',label:'组数',unit:'组',type:'count'})}else{if(e?.duration!=null)legacy.push({key:'duration',label:'时间',unit:'分钟',type:'duration'});if(e?.intensity!=null)legacy.push({key:'intensity',label:'强度',unit:'/10',type:'rating'})}schema=legacy}
 return(schema||[]).map(m=>{const x=axis818CloneMetric?axis818CloneMetric(m):m,v=axis821MetricValueForEvent(e,x),text=axis821MetricValueText(x,v);return{key:String(x?.key||''),label:String(x?.label||x?.key||''),unit:String(x?.unit||''),type:String(x?.type||''),value:v,text}}).filter(x=>x.text)
}
function axis821EventMetricSummary(e){const xs=axis821EventMetricEntries(e);if(!xs.length)return'已记录';return xs.slice(0,3).map(x=>{if(x.key==='weight')return x.text;if(x.key==='reps'||x.key==='sets'||x.key==='duration')return x.text.replace(/\s+/g,'');if(x.key==='intensity')return'强度 '+x.text;return x.label+' '+x.text}).join(' · ')+(xs.length>3?' · …':'')}
function axis821EventSetCount(e){const mode=String(e?.executionModeSnapshot||'');if(mode!=='sets'&&mode!=='rounds'){const schema=Array.isArray(e?.metricSchemaSnapshot)?e.metricSchemaSnapshot:null;if(schema){const keys=new Set(schema.map(x=>x?.key||x?.id));if(!keys.has('sets')&&!keys.has('rounds')&&!(keys.has('weight')&&keys.has('reps')))return 0}}const n=Number(e?.sets);if(Number.isFinite(n)&&n>0)return n;try{const m=JSON.parse(localStorage.getItem('axis_v8_meta')||'{}'),xs=m.events?.[e?.id]?.sets;return Array.isArray(xs)?xs.filter(x=>x?.state!=='unfinished').length:0}catch{return 0}}
function axis821SessionExecutionStats(events){const xs=events||[],sets=xs.reduce((n,e)=>n+axis821EventSetCount(e),0);return{sets,activities:xs.length}}
function axis821VisibleObjectType(v){return({strength:'力量',cardio:'有氧',relative:'自重',machine:'器械力量',free:'自由重量',bodyweight:'自重训练',functional:'功能体能',mobility:'灵活恢复'}[String(v||'')]||String(v||''))}
`;
 const eventAnchor='function eventMeta(e)';if(!s.includes(eventAnchor))fail('history formatter anchor missing');s=s.replace(eventAnchor,historyHelpers+eventAnchor);
 s=replaceFunction(s,'function eventMeta(e)',`function eventMeta(e){return axis821EventMetricSummary(e)}`,'schema-aware event summary');
 s=replaceFunction(s,'function renderLive()',`function renderLive(){const a=ev(state.active),stats=axis821SessionExecutionStats(a);setText('#liveItems',a.length);setText('#liveSets',stats.sets||stats.activities);const liveLabel=$('#liveSets')?.parentElement?.querySelector('span');if(liveLabel)liveLabel.textContent=stats.sets?'总组数':'活动项';setText('#eventCount',a.length);$('#eventList').innerHTML=a.length?a.slice().reverse().map(eventHtml).join(''):'<div class="empty">暂无记录</div>';const last=a.length?a[a.length-1].time:state.active.start,idle=(Date.now()-last)/60000;$('#liveSignalWrap').classList.toggle('hidden',idle<7);if(idle>=7){setText('#pace',idle>=10?'停得有点久':'休息偏久');$('#liveSignal').innerHTML='<strong>'+Math.floor(idle)+'分钟没有新记录</strong><small>继续训练或长按结束</small>'}}`,'Today execution-aware aggregates');
 s=replaceFunction(s,'async function openEvent(id)',`async function openEvent(id){const e=allEvents().find(x=>x.id===id);if(!e)return;const eq=eventEq(e);setText('#detailTitle',e.name);let media='';if(e.clipRef){const v=await mediaUrl(e.clipRef),poster=e.frameRefs?.[0]?await mediaUrl(e.frameRefs[0]):'';if(v)media+='<div class="videoCard"><div class="mediaTop"><b>现场片段</b><span>'+(e.videoWatermarked?'带水印':'原片')+'</span></div><video class="detailVideo" src="'+v+'" '+(poster?'poster="'+poster+'"':'')+' controls playsinline muted></video><button class="mediaSave" id="saveDetailVideo">保存视频</button></div>'}if(e.frameRefs?.length){const imgs=[];for(const r of e.frameRefs){const u=await mediaUrl(r);if(u)imgs.push('<img src="'+u+'" alt="现场照片">')}media+='<div class="mediaTop photoTop"><b>现场照片</b><span>'+imgs.length+'张 · '+(state.prefs.watermark.photoMode==='raw'?'原片':'带水印')+'</span></div><div class="detailFilm">'+imgs.join('')+'</div>'}const rows=[['主要锻炼',(e.muscles||eq.muscles||[]).join(' · ')||'—'],...axis821EventMetricEntries(e).map(x=>[x.label,x.text]),['记录时间',dlabel(e.time)+' '+tlabel(e.time)]];$('#detail').innerHTML=media+'<div class="detailList">'+rows.map(r=>'<div class="detailRow"><span>'+esc(r[0])+'</span><span>'+esc(r[1])+'</span></div>').join('')+'</div>'+(e.frameRefs?.[0]?'<div class="detailActions"><button id="saveDetailPhoto">保存图片</button><button id="makeSessionReport">训练报告</button></div>':'');openSheet('detailSheet');if($('#saveDetailPhoto'))$('#saveDetailPhoto').onclick=async()=>{const b=await getMedia(e.frameRefs[0]);if(b)shareBlob(b,'AXIS-'+e.name+'-'+dlabel(e.time)+'.jpg','image/jpeg')};if($('#saveDetailVideo'))$('#saveDetailVideo').onclick=async()=>{const b=await getMedia(e.clipRef);if(b)shareBlob(b,'AXIS-'+e.name+'-'+dlabel(e.time)+'.'+((b.type||'video/mp4').includes('webm')?'webm':'mp4'),b.type||'video/mp4')};if($('#makeSessionReport'))$('#makeSessionReport').onclick=()=>{const ss=(state.active?[state.active]:[]).concat(state.sessions).find(s=>ev(s).some(x=>x.id===e.id));if(ss){reportRange='session:'+ss.id;renderReport();openSheet('reportSheet')}}}`,'schema-aware event detail');
 s=replaceFunction(s,'function openSession(id)',`function openSession(id){const ss=state.sessions.find(x=>x.id===id);if(!ss)return;const stats=axis821SessionExecutionStats(ev(ss)),thirdLabel=stats.sets?'组数':'记录';setText('#detailTitle',dlabel(ss.start));$('#detail').innerHTML='<div class="detailList"><div class="detailRow"><span>训练时间</span><span>'+mins(ss)+' 分钟</span></div><div class="detailRow"><span>项目</span><span>'+ev(ss).length+'</span></div><div class="detailRow"><span>'+thirdLabel+'</span><span>'+(stats.sets||stats.activities)+'</span></div></div><div class="events">'+ev(ss).slice().reverse().map(eventHtml).join('')+'</div><div class="detailActions"><button id="sessionReportBtn">训练报告</button><button id="sessionDeleteBtn">删除本次</button></div>';openSheet('detailSheet');bindDynamic();hydrateThumbs();$('#sessionReportBtn').onclick=()=>{reportRange='session:'+ss.id;renderReport();openSheet('reportSheet')};$('#sessionDeleteBtn').onclick=async()=>{if(!confirm('删除这次训练？'))return;await deleteSessions([ss.id]);closeSheet('detailSheet');render();toast('已删除')}}`,'schema-aware session detail');
 s=replaceFunction(s,'function completeFinish()',`function completeFinish(){finishCancelHold();if(!state.active)return;state.active.end=Date.now();const ss=state.active,stats=axis821SessionExecutionStats(ev(ss));state.sessions.unshift(ss);state.active=null;save();setText('#finishDuration',mins(ss)+'分钟');setText('#finishItems',ev(ss).length);setText('#finishSets',stats.sets||stats.activities);const label=$('#finishSets')?.parentElement?.querySelector('span');if(label)label.textContent=stats.sets?'组':'记录';openSheet('finishSheet');render()}`,'execution-aware session finish summary');

 const enumBridge=String.raw`
function axis821LocalizeVisibleEnums(root=D){for(const el of root.querySelectorAll?.('small,span')||[]){const t=(el.textContent||'').trim();if(['strength','cardio','relative','machine','free','bodyweight','functional','mobility'].includes(t))el.textContent=axis821VisibleObjectType(t)}}
const axis821EnumObserver=new MutationObserver(ms=>{for(const m of ms)for(const n of m.addedNodes||[])if(n?.nodeType===1)axis821LocalizeVisibleEnums(n)});axis821EnumObserver.observe(D.body,{subtree:true,childList:true});queueMicrotask(()=>axis821LocalizeVisibleEnums(D));
`;
 const marker="try{window.__AXIS_821_PRODUCT_CONVERGENCE__=";const markerAt=s.indexOf(marker);if(markerAt<0)fail('8.21 product marker missing');s=s.slice(0,markerAt)+enumBridge+s.slice(markerAt);
 s=replaceFunction(s,'function axis8201ApplyObjectSchemaChange(event)',`function axis8201ApplyObjectSchemaChange(event){const d=event?.detail||{},eq=(state.profile?.customEq||[]).find(x=>x.id===d.id);if(!eq||!Array.isArray(d.schema))return false;eq.metricSchema=d.schema.map(axis818CloneMetric);eq.metricSchemaVersion=String(d.metricSchemaVersion||'8.21');const mode=String(d.executionMode||d.recording?.executionMode||eq.executionMode||'auto');eq.executionMode=mode;eq.recording=Object.assign({},eq.recording||{},d.recording||{},{version:2,metrics:eq.metricSchema.map(x=>x.key),executionMode:mode});save();if(state.selectedEq===eq.id){const host=$('#axis818MetricRecorder');if(host)host.dataset.axis818RenderKey='';axis818RenderRecorder()}try{render()}catch{}return true}`,'live Object schema + execution sync');
 s+=`\ntry{window.__AXIS_821_EXECUTABLE_OBJECT_SYSTEM__={version:'8.21',objectContract:'schema+execution',executionAuto:true,continuousAutoTimed:true,historyFromEncounterSnapshot:true,visibleEnumLocalized:true,newStorage:false,newDatabase:false,newRecorder:false,newPicker:false,newActiveOwner:false,newEncounterWriter:false}}catch{}\n`;
 syntax(s,FILE);write(FILE,s);
}

/* v874 — compact Object configuration UI; same schema draft/persistence owner. */
{
 const FILE='v874-professional.js';let s=read(FILE);
 const state="let axis818MetricDraft=[],axis821CustomMetricOpen=false,axis821CustomMetricType='number';";
 s=once(s,state,"let axis818MetricDraft=[],axis821CustomMetricOpen=false,axis821CustomMetricType='number',axis821ExecutionDraft='auto';",'Object execution draft state');
 s=replaceFunction(s,'function axis818MetricLoad()',`function axis818MetricLoad(){const c=readCore(),eq=editId?(c.profile?.customEq||[]).find(x=>x.id===editId):null,hasExplicit=!!eq&&Array.isArray(eq.metricSchema);axis818MetricDraft=(hasExplicit?eq.metricSchema:axis818MetricDefault(eq?.type||($('#customType .active')?.dataset.type)||'strength')).map(x=>({...x}));axis821ExecutionDraft=String(eq?.executionMode||eq?.recording?.executionMode||'auto');if(!['auto','single','sets','rounds','timed','hold','complete'].includes(axis821ExecutionDraft))axis821ExecutionDraft='auto';axis821CustomMetricOpen=false;axis818MetricRender()}`,'Object editor schema + execution hydration');
 const uiHelpers=String.raw`
function axis821EditorAutoMode(){const keys=new Set(axis818MetricDraft.map(x=>x.key));if(keys.has('hold'))return'hold';if(keys.has('sets')||keys.has('rounds')||(keys.has('weight')&&keys.has('reps')))return'sets';if(keys.has('completed')&&keys.size===1)return'complete';if(['duration','distance','pace','speed','intensity','resistance','level','incline'].some(k=>keys.has(k)))return'timed';return'single'}
function axis821EditorModeLabel(v){return({auto:'自动',single:'一次记录',sets:'分组进行',rounds:'循环轮次',timed:'连续计时',hold:'保持计时',complete:'完成即记'}[v]||'自动')}
function axis821EditorResolvedMode(){return axis821ExecutionDraft==='auto'?axis821EditorAutoMode():axis821ExecutionDraft}
function axis821MetricPickerEnsure(){if(!$('#axis821MetricPickerSheet'))D.body.insertAdjacentHTML('beforeend','<div class="sheetWrap" id="axis821MetricPickerSheet"><div class="sheet axis821MetricPickerSheet"><div class="grabber"></div><div class="sheetHead"><b>记录属性</b><button class="closeBtn" data-axis821-property-close>×</button></div><div id="axis821MetricPickerBody"></div></div></div>');if(!$('#axis821ExecutionSheet'))D.body.insertAdjacentHTML('beforeend','<div class="sheetWrap" id="axis821ExecutionSheet"><div class="sheet axis821ExecutionSheet"><div class="grabber"></div><div class="sheetHead"><b>进行方式</b><button class="closeBtn" data-axis821-execution-close>×</button></div><div id="axis821ExecutionBody"></div></div></div>')}
function axis821MetricPickerBody(){const body=$('#axis821MetricPickerBody');if(!body)return;const has=k=>axis818MetricDraft.some(x=>x.key===k),groups=[['训练量',['weight','reps','sets']],['时间与移动',['duration','hold','distance','pace','speed']],['强度与设备',['intensity','resistance','level','incline','rating','completed']]],custom=axis818MetricDraft.map((x,i)=>({x,i})).filter(o=>o.x.custom);body.innerHTML='<div class="axis821PickerHint">只选择这个项目真正需要记录的内容</div>'+groups.map(g=>'<section class="axis821PickerGroup"><small>'+g[0]+'</small><div>'+g[1].map(k=>{const x=AXIS818_METRIC_CHOICES.find(v=>v[0]===k);return'<button type="button" data-axis818-metric-choice="'+k+'" class="'+(has(k)?'active':'')+'"><span>'+esc(x[1])+'</span><i>'+(has(k)?'✓':'')+'</i></button>'}).join('')+'</div></section>').join('')+(custom.length?'<section class="axis821PickerGroup"><small>自定义</small><div>'+custom.map(o=>'<button type="button" data-axis818-metric-remove="'+o.i+'" class="active"><span>'+esc(o.x.label)+'</span><i>×</i></button>').join('')+'</div></section>':'')+'<button class="axis821PickerCustom" type="button" data-axis818-metric-custom="1">＋ 自定义记录属性</button>'+(axis821CustomMetricOpen?'<div class="axis821CustomMetric"><div class="axis821CustomMetricFields"><input id="axis821CustomMetricName" autocomplete="off" placeholder="属性名称"><input id="axis821CustomMetricUnit" autocomplete="off" placeholder="单位（可留空）"></div><div class="axis821CustomMetricTypes">'+[['number','数值'],['count','次数'],['duration','时间'],['rating','1–10'],['percentage','百分比'],['boolean','是 / 否'],['pace','配速']].map(x=>'<button type="button" data-axis821-custom-type="'+x[0]+'" class="'+(axis821CustomMetricType===x[0]?'active':'')+'">'+x[1]+'</button>').join('')+'</div><button type="button" class="axis821CustomMetricAdd" data-axis821-custom-add="1">添加属性</button></div>':'')}
function axis821ExecutionBody(){const body=$('#axis821ExecutionBody');if(!body)return;const auto=axis821EditorAutoMode(),modes=[['auto','自动','根据记录属性判断 · '+axis821EditorModeLabel(auto)],['sets','分组进行','重量 / 次数 / 组数'],['timed','连续计时','速度 / 坡度 / 距离 / 强度'],['hold','保持计时','保持时间'],['rounds','循环轮次','按轮次推进'],['single','一次记录','记下即完成'],['complete','完成即记','只记录是否完成']];body.innerHTML='<div class="axis821ExecutionChoices">'+modes.map(x=>'<button type="button" data-axis821-execution-choice="'+x[0]+'" class="'+(axis821ExecutionDraft===x[0]?'active':'')+'"><span><b>'+x[1]+'</b><small>'+x[2]+'</small></span><i>'+(axis821ExecutionDraft===x[0]?'✓':'')+'</i></button>').join('')+'</div>'}
`;
 const renderAnchor='function axis818MetricRender()';s=s.replace(renderAnchor,uiHelpers+renderAnchor);
 s=replaceFunction(s,'function axis818MetricRender()',`function axis818MetricRender(){const host=axis818MetricEnsure();if(!host)return;axis821MetricPickerEnsure();const names=axis818MetricDraft.map(x=>x.label),shown=names.slice(0,4),more=Math.max(0,names.length-shown.length),resolved=axis821EditorResolvedMode();host.innerHTML='<div class="axis821ObjectConfig"><button type="button" class="axis821ObjectConfigRow" data-axis821-property-open><span><b>记录属性</b><small>'+(names.length?(shown.join(' · ')+(more?' · +'+more:'')):'不记录数值')+'</small></span><em>已选 '+names.length+' 项</em><i>›</i></button><button type="button" class="axis821ObjectConfigRow" data-axis821-execution-open><span><b>进行方式</b><small>'+axis821EditorModeLabel(resolved)+'</small></span><em>'+(axis821ExecutionDraft==='auto'?'自动':'手动')+'</em><i>›</i></button></div>';axis821MetricPickerBody();axis821ExecutionBody()}`,'compact Object recording/execution configuration');
 s=replaceFunction(s,'function axis818MetricPersist()',`function axis818MetricPersist(){const name=$('#customName')?.value.trim()||'',targetId=editId,draft=axis818MetricDraft.map(x=>({...x})),executionMode=axis821ExecutionDraft||'auto';const commit=(attempt=0)=>{const cc=readCore(),xs=cc.profile?.customEq||[],eq=(targetId&&xs.find(x=>x.id===targetId))||xs.filter(x=>x.name===name).at(-1);if(!eq){if(attempt<20)setTimeout(()=>commit(attempt+1),16);return}eq.metricSchema=draft.map(x=>({...x}));eq.metricSchemaVersion='8.21';eq.executionMode=executionMode;eq.recording=Object.assign({},eq.recording||{},{version:2,metrics:draft.map(x=>x.key),executionMode});writeCore(cc);window.dispatchEvent(new CustomEvent('axis:object-schema-changed',{detail:{id:eq.id,schema:eq.metricSchema.map(x=>({...x})),metricSchemaVersion:eq.metricSchemaVersion,executionMode,recording:eq.recording}}))};setTimeout(()=>commit(),0)}`,'Object schema + execution persistence');
 let end=s.lastIndexOf('})();');if(end<0)fail('v874 IIFE end missing');const events=String.raw`
D.addEventListener('click',e=>{if(e.target.closest('[data-axis821-property-open]')){axis821MetricPickerBody();$('#axis821MetricPickerSheet')?.classList.add('show');return}if(e.target.closest('[data-axis821-property-close]')||e.target===$('#axis821MetricPickerSheet')){$('#axis821MetricPickerSheet')?.classList.remove('show');return}if(e.target.closest('[data-axis821-execution-open]')){axis821ExecutionBody();$('#axis821ExecutionSheet')?.classList.add('show');return}if(e.target.closest('[data-axis821-execution-close]')||e.target===$('#axis821ExecutionSheet')){$('#axis821ExecutionSheet')?.classList.remove('show');return}const mode=e.target.closest('[data-axis821-execution-choice]');if(mode){axis821ExecutionDraft=mode.dataset.axis821ExecutionChoice||'auto';axis821ExecutionBody();axis818MetricRender();return}},true);
`;
 s=s.slice(0,end)+events+s.slice(end);syntax(s,FILE);write(FILE,s);
}

/* app recorder — one stable control geometry for every metric family. */
{
 const FILE='app.js';let s=read(FILE);
 s=replaceFunction(s,'function axis821MetricControl(m,prev)',`function axis821MetricControl(m,prev){const kind=axis821MetricKind(m),key=esc(m.key),label=esc(m.label),unit=esc(m.unit||''),value=axis821MetricValue(prev),step=Number(m.step)>0?Number(m.step):1,min=Number.isFinite(Number(m.min))?Number(m.min):(kind==='rating'?1:0),max=Number.isFinite(Number(m.max))?Number(m.max):(kind==='rating'?10:'');const head='<div class="axis821MetricLabel"><span>'+label+'</span><small>'+unit+'</small></div>';if(kind==='rating'){const current=Number(value)||0;return'<section class="axis821MetricControl" data-axis821-kind="rating" data-axis821-key="'+key+'">'+head+'<div class="axis821Direct axis821RatingDirect"><input data-axis818-metric="'+key+'" inputmode="numeric" autocomplete="off" value="'+esc(value)+'" placeholder="—" min="1" max="10"><small>/10</small></div><div class="axis821Rating">'+Array.from({length:10},(_,i)=>i+1).map(n=>'<button type="button" data-axis821-rate="'+key+'" data-value="'+n+'" class="'+(current===n?'active':'')+'">'+n+'</button>').join('')+'</div></section>'}if(kind==='toggle'){const on=value===''?true:(value==='true'||value==='1');return'<section class="axis821MetricControl" data-axis821-kind="toggle" data-axis821-key="'+key+'">'+head+'<input type="hidden" data-axis818-metric="'+key+'" value="'+(on?'1':'0')+'"><div class="axis821Toggle"><button type="button" data-axis821-bool="'+key+'" data-value="0" class="'+(!on?'active':'')+'">否</button><button type="button" data-axis821-bool="'+key+'" data-value="1" class="'+(on?'active':'')+'">是</button></div></section>'}if(kind==='pace'){return'<section class="axis821MetricControl" data-axis821-kind="pace" data-axis821-key="'+key+'">'+head+'<div class="axis821Direct"><input data-axis818-metric="'+key+'" inputmode="text" autocomplete="off" value="'+esc(value)+'" placeholder="5:30"><small>'+(unit||'min/km')+'</small></div></section>'}const presets=kind==='timer'?(m.key==='hold'?[10,20,30,45,60,90]:[5,10,15,20,30,45]):m.key==='distance'?[.5,1,3,5,10]:m.key==='speed'?[5,8,10,12,15]:m.key==='incline'?[0,2,5,8,10,12]:[];return'<section class="axis821MetricControl" data-axis821-kind="'+kind+'" data-axis821-key="'+key+'">'+head+'<div class="axis821Stepper"><button type="button" aria-label="减少'+label+'" data-axis821-step="'+key+'" data-delta="-'+step+'">−</button><div><input data-axis818-metric="'+key+'" inputmode="decimal" autocomplete="off" value="'+esc(value)+'" placeholder="—" data-min="'+min+'" '+(max!==''?'data-max="'+max+'"':'')+'><small>'+unit+'</small></div><button type="button" aria-label="增加'+label+'" data-axis821-step="'+key+'" data-delta="'+step+'">＋</button></div>'+(presets.length?'<div class="axis821Presets">'+presets.map(n=>'<button type="button" data-axis821-preset="'+key+'" data-value="'+n+'">'+n+'</button>').join('')+'</div>':'')+'</section>'}`,'engineered canonical metric control');
 syntax(s,FILE);write(FILE,s);
}

/* Static CSS only. Existing design tokens; no runtime geometry owner. */
{
 const FILE='styles.css';let s=read(FILE),marker='/* AXIS 8.21 Executable Object System */';if(s.includes(marker))fail('Executable Object CSS duplicated');
 s+=`\n\n${marker}\n.axis821ObjectConfig{border-top:1px solid var(--line2);margin-top:10px}.axis821ObjectConfigRow{width:100%;min-height:64px;padding:10px 2px;border:0;border-bottom:1px solid var(--line2);background:transparent;display:grid;grid-template-columns:minmax(0,1fr) auto 18px;gap:10px;align-items:center;text-align:left}.axis821ObjectConfigRow>span{min-width:0}.axis821ObjectConfigRow b{display:block;color:var(--text);font-size:13px;font-weight:650}.axis821ObjectConfigRow small{display:block;margin-top:5px;color:var(--muted);font-size:10.5px;line-height:1.4;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.axis821ObjectConfigRow em{font-style:normal;color:var(--dim);font-size:10.5px}.axis821ObjectConfigRow>i{font-style:normal;color:var(--dim);font-size:18px}.axis821MetricPickerSheet,.axis821ExecutionSheet{padding-bottom:max(18px,env(safe-area-inset-bottom))}.axis821PickerHint{padding:4px 2px 16px;color:var(--dim);font-size:10.5px}.axis821PickerGroup{margin:0 0 18px}.axis821PickerGroup>small{display:block;margin:0 2px 9px;color:var(--dim);font-size:10px}.axis821PickerGroup>div{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.axis821PickerGroup button{height:46px;padding:0 10px;border-radius:14px;background:var(--s2);color:var(--muted);display:flex;align-items:center;justify-content:center;gap:6px;font-size:11.5px;font-weight:580}.axis821PickerGroup button.active{background:rgba(115,124,255,.14);color:var(--accent2);box-shadow:inset 0 0 0 1px rgba(115,124,255,.24)}.axis821PickerGroup button i{font-style:normal;font-size:10px}.axis821PickerCustom{width:100%;height:52px;border-top:1px solid var(--line2);border-bottom:1px solid var(--line2);color:var(--accent2);text-align:left;font-size:12px}.axis821MetricPickerSheet .axis821CustomMetric{margin:12px 0 0!important}.axis821ExecutionChoices{border-top:1px solid var(--line2)}.axis821ExecutionChoices>button{width:100%;min-height:62px;padding:9px 2px;border-bottom:1px solid var(--line2);display:grid;grid-template-columns:minmax(0,1fr) 24px;gap:12px;align-items:center;text-align:left}.axis821ExecutionChoices b{display:block;font-size:13px;color:var(--text)}.axis821ExecutionChoices small{display:block;margin-top:4px;font-size:10px;color:var(--dim)}.axis821ExecutionChoices i{font-style:normal;text-align:right;color:var(--accent2)}.axis821ExecutionChoices>button.active b{color:var(--accent2)}@media(max-width:390px){.axis821PickerGroup>div{grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.axis821PickerGroup button{height:44px;padding:0 7px;font-size:11px}}\n`;
 write(FILE,s);
}

for(const [f,tokens] of [
 ['app.js',['__AXIS_821_EXECUTABLE_OBJECT_SYSTEM__','axis821AutoExecutionMode','AXIS821_CONTINUOUS_METRICS','axis821EventMetricEntries','axis821VisibleObjectType','continuousAutoTimed:true']],
 ['v874-professional.js',['axis821ExecutionDraft','axis821MetricPickerSheet','data-axis821-execution-choice','executionMode=axis821ExecutionDraft']],
 ['styles.css',['AXIS 8.21 Executable Object System','.axis821ObjectConfigRow','.axis821PickerGroup','.axis821ExecutionChoices']]
]){const x=read(f);for(const t of tokens)if(!x.includes(t))fail(`${f} missing ${t}`)}
{const css=read('styles.css'),marker='/* AXIS 8.21 Executable Object System */',at=css.indexOf(marker);if(at<0)fail('Executable Object CSS marker missing');const owned=css.slice(at);for(const selector of ['.axis818MetricRecorder{','.axis821MetricControl{','.axis821MetricLabel{','.axis821Stepper{','.axis821Stepper>button{','.axis821Stepper>div{','.axis821Stepper input{','.axis821Presets{','.axis821Rating{','.axis821Toggle{','.axis821Direct{','.axis821NoMetrics{'])if(owned.includes(selector))fail(`Executable Object must not own recorder geometry: ${selector}`)}
const app=read('app.js');if((app.match(/state\.active\.events\.push\(/g)||[]).length!==1)fail('Encounter append ownership drift');for(const forbidden of ['axis_exec_state','axis_object_runtime_state',"localStorage.setItem('axis_exec","localStorage.setItem(\"axis_exec"])if(app.includes(forbidden))fail(`forbidden persistence owner ${forbidden}`);
console.log('[AXIS 8.21 Executable Object System] PASS · schema+execution Object contract · continuous metrics auto-timed · recorder geometry remains metric-control-system-owned · same recorder/Encounter/Active owners · snapshot-aware history · compact property/execution UI · no new storage');
