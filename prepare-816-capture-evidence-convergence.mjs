import fs from 'node:fs';

const INDEX='index.html',APP='app.js',MEDIA='v815-media-evidence.js',CSS='styles.css';
const fail=m=>{throw new Error(`[AXIS 8.16 Capture + Evidence convergence] ${m}`)};
const read=f=>{if(!fs.existsSync(f))fail(`missing ${f}`);return fs.readFileSync(f,'utf8')};
const write=(f,s)=>fs.writeFileSync(f,s);
const once=(src,from,to,label)=>{const n=src.split(from).length-1;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(from,to)};
const regexOnce=(src,re,to,label)=>{const flags=re.flags.includes('g')?re.flags:re.flags+'g',n=(src.match(new RegExp(re.source,flags))||[]).length;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(re,()=>to)};

/* One visible Capture Field, while the historical controls remain hidden compatibility
   anchors for inherited capture preference and diagnostics. No new persistence surface. */
{
 let html=read(INDEX);
 html=once(html,'<div class="sheetWrap" id="scanSheet"><div class="sheet">','<div class="sheetWrap" id="scanSheet" data-axis-capture-owner="app.js" data-axis-capture-surface="v816-capture-field"><div class="sheet">','capture ownership');
 html=once(html,'  <div id="captureStage">','  <div id="captureStage" class="v816CaptureField" data-axis-capture-surface="v816-capture-field">','capture field root');
 const anchor='    <div class="captureModes" id="captureModes">';
 const chrome=`    <div class="v816CaptureMeta"><span id="v816CaptureContext">现场证据</span><b id="v816CaptureMeta">0 / 12</b></div>\n    <div class="v816CaptureMode" id="v816CaptureMode" role="tablist" aria-label="拍摄方式"><button type="button" role="tab" data-v816-mode="photo" aria-selected="true">照片</button><button type="button" role="tab" data-v816-mode="scan" aria-selected="false">扫描</button><button type="button" role="tab" data-v816-mode="video" aria-selected="false">视频</button></div>\n    <div class="v816CaptureActions"><div class="v816CaptureSide"><span id="v816CaptureHint">可连续拍摄</span><small id="v816CaptureLimit">最多 12 张</small></div><button type="button" class="v816Shutter" id="v816Shutter" aria-label="拍摄"><i></i><span id="v816ShutterText">拍照</span></button><button type="button" class="v816CaptureDone" id="v816CaptureDone" disabled>完成</button></div>\n    <div class="v816DraftRail" id="v816DraftRail" aria-label="本次影像草稿"></div>\n`;
 html=once(html,anchor,chrome+anchor,'Capture Field controls');
 html=once(html,'<input id="photoInput" class="hiddenInput" type="file" accept="image/*" capture="environment">','<input id="photoInput" class="hiddenInput" type="file" accept="image/*" capture="environment" multiple>','multi-photo fallback');
 write(INDEX,html);
}

{
 let css=read(CSS);
 if(css.includes('AXIS 8.16 Capture Field'))fail('8.16 Capture CSS already installed');
 css+=`\n\n/* AXIS 8.16 Capture Field + Comparative Evidence */
#scanSheet[data-axis-capture-surface="v816-capture-field"] .sheet{overflow-x:hidden}
#captureStage.v816CaptureField{position:relative;padding-bottom:max(10px,env(safe-area-inset-bottom))}
#captureStage.v816CaptureField .camera{position:relative;height:clamp(320px,52dvh,540px);border-radius:24px;overflow:hidden;background:#05070a;isolation:isolate}
#captureStage.v816CaptureField .camera:before{content:"";position:absolute;inset:0;z-index:2;pointer-events:none;background:linear-gradient(90deg,transparent 33.2%,rgba(255,255,255,.055) 33.3%,rgba(255,255,255,.055) 33.55%,transparent 33.65%,transparent 66.3%,rgba(255,255,255,.055) 66.4%,rgba(255,255,255,.055) 66.65%,transparent 66.75%),linear-gradient(0deg,transparent 33.2%,rgba(255,255,255,.045) 33.3%,rgba(255,255,255,.045) 33.55%,transparent 33.65%,transparent 66.3%,rgba(255,255,255,.045) 66.4%,rgba(255,255,255,.045) 66.65%,transparent 66.75%);opacity:.42}
#captureStage.v816CaptureField .camera video{width:100%;height:100%;object-fit:cover}
#captureStage.v816CaptureField #scanState{z-index:3;backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px)}
.v816CaptureMeta{display:flex;align-items:center;justify-content:space-between;gap:14px;margin:13px 2px 9px}.v816CaptureMeta span{min-width:0;color:#8e96a2;font-size:11px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.v816CaptureMeta b{flex:0 0 auto;color:#c9ced6;font-size:10px;font-weight:600;font-variant-numeric:tabular-nums}
.v816CaptureMode{display:grid;grid-template-columns:repeat(3,1fr);gap:3px;padding:3px;border-radius:14px;background:#0e1116}.v816CaptureMode button{appearance:none;border:0;height:36px;border-radius:11px;background:transparent;color:#747d8a;font:600 11px/1 -apple-system,BlinkMacSystemFont,'PingFang SC',sans-serif}.v816CaptureMode button[aria-selected="true"]{background:#191e27;color:#f0f1f3;box-shadow:0 1px 0 rgba(255,255,255,.04) inset}
.v816CaptureActions{display:grid;grid-template-columns:minmax(0,1fr) 88px minmax(0,1fr);align-items:center;gap:8px;min-height:104px}.v816CaptureSide{min-width:0;padding-left:2px}.v816CaptureSide span,.v816CaptureSide small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.v816CaptureSide span{color:#c6cbd3;font-size:11px}.v816CaptureSide small{margin-top:4px;color:#69727f;font-size:9px}.v816Shutter{position:relative;appearance:none;border:0;width:76px;height:76px;margin:auto;border-radius:50%;background:transparent;box-shadow:inset 0 0 0 2px #e7e9ed;display:grid;place-items:center}.v816Shutter i{width:60px;height:60px;border-radius:50%;background:#f2f2f0;transition:transform .16s ease,border-radius .16s ease,width .16s ease,height .16s ease,background .16s ease}.v816Shutter span{position:absolute;inset:auto 0 -18px;color:#737c88;font-size:8px;text-align:center;white-space:nowrap}.v816Shutter[data-recording="1"]{box-shadow:inset 0 0 0 2px #ff675f}.v816Shutter[data-recording="1"] i{width:28px;height:28px;border-radius:8px;background:#ff675f}.v816Shutter:disabled{opacity:.35}.v816CaptureDone{justify-self:end;appearance:none;border:0;background:transparent;color:#d8dcff;font:650 11px/1 -apple-system,BlinkMacSystemFont,'PingFang SC',sans-serif;padding:14px 2px 14px 18px}.v816CaptureDone:disabled{color:#505761}
.v816DraftRail{display:flex;gap:7px;min-height:0;overflow-x:auto;scrollbar-width:none;padding:1px 1px 4px}.v816DraftRail::-webkit-scrollbar{display:none}.v816DraftItem{position:relative;flex:0 0 58px;height:72px;border-radius:13px;overflow:hidden;background:#0d1117}.v816DraftItem>button:first-child{appearance:none;border:0;width:100%;height:100%;padding:0;background:transparent}.v816DraftItem img{width:100%;height:100%;display:block;object-fit:cover}.v816DraftItem em{position:absolute;left:5px;bottom:5px;padding:3px 5px;border-radius:999px;background:rgba(5,7,10,.72);color:#e9ebef;font:600 7px/1 -apple-system,BlinkMacSystemFont,sans-serif;font-style:normal}.v816DraftItem .v816Remove{position:absolute;right:3px;top:3px;width:22px;height:22px;border:0;border-radius:50%;background:rgba(5,7,10,.72);color:#eef0f2;font-size:14px;line-height:22px}.v816DraftVideo{display:grid;place-items:center;background:#111722;color:#b9c1cf;font-size:9px}.v816DraftVideo b{display:block;color:#eef0f4;font-size:10px;font-variant-numeric:tabular-nums}.v816DraftEmpty{display:flex;align-items:center;height:32px;color:#4f5864;font-size:9px}
#captureStage.v816CaptureField>#captureModes,#captureStage.v816CaptureField>#captureNow{display:none!important}
#reviewStage #film video{display:block;width:100%;max-height:280px;object-fit:cover;border-radius:15px;background:#080a0d}
.v816CompareTools{display:flex;align-items:center;gap:5px;padding:8px 9px;background:#0d1117;overflow-x:auto;scrollbar-width:none}.v816CompareTools::-webkit-scrollbar{display:none}.v816CompareTools button{flex:0 0 auto;appearance:none;border:0;border-radius:999px;min-height:27px;padding:0 9px;background:#151a22;color:#798291;font:600 9px/1 -apple-system,BlinkMacSystemFont,'PingFang SC',sans-serif}.v816CompareTools button[aria-pressed="true"],.v816CompareTools button[data-active="1"]{background:#202638;color:#e1e4ff}.v816CompareMeta{margin-left:auto;flex:0 0 auto;color:#68717e;font-size:8px;font-variant-numeric:tabular-nums}.v815Rail button[data-v816-pair="1"]{box-shadow:inset 0 0 0 1px rgba(151,160,255,.42)}
@media(max-width:390px){#captureStage.v816CaptureField .camera{height:clamp(300px,49dvh,470px)}.v816CaptureActions{grid-template-columns:minmax(0,1fr) 82px minmax(0,1fr)}.v816Shutter{width:70px;height:70px}.v816Shutter i{width:54px;height:54px}}
@media(prefers-reduced-motion:reduce){.v816Shutter i{transition:none}}
`;
 write(CSS,css);
}

{
 let app=read(APP);
 app=once(app,
  "let timer=null,toastTimer=null,finishRaf=null,finishStart=0,captureMode='3',editCustomId=null,reportRange='last',selectedSessions=new Set();",
  "let timer=null,toastTimer=null,finishRaf=null,finishStart=0,captureMode='3',editCustomId=null,reportRange='last',selectedSessions=new Set();\nconst CAPTURE816_PHOTO_MAX=12,CAPTURE816_VIDEO_MAX_MS=60000;let capture816UiMode='photo',capture816Busy=false,capture816Recorder=null,capture816Chunks=[],capture816StopTimer=null,capture816TickTimer=null,capture816StartedAt=0,capture816Discard=false,capture816StopWaiters=[];",
  'Capture Field transient state');
 app=once(app,
  "function closeSheet(id){$('#'+id)?.classList.remove('show');if(id==='scanSheet')stopCamera()}",
  "function closeSheet(id){$('#'+id)?.classList.remove('show');if(id==='scanSheet'){capture816AbortVideo(true);stopCamera()}}",
  'camera close safety');

 const captureFns=`
function capture816Duration(){return capture816Recorder?.state==='recording'?Math.min(CAPTURE816_VIDEO_MAX_MS,Math.max(0,Date.now()-capture816StartedAt)):Math.max(0,Number(state.clip?.durationMs)||0)}
function capture816DraftSnapshot(){return{mode:capture816UiMode,recording:capture816Recorder?.state==='recording',busy:capture816Busy,maxPhotos:CAPTURE816_PHOTO_MAX,maxVideoMs:CAPTURE816_VIDEO_MAX_MS,photos:state.frames.map((f,i)=>({index:i,url:f.url,ts:f.ts||0})),video:state.clip?{url:state.clip.url||'',type:state.clip.type||'',durationMs:capture816Duration()}:null,durationMs:capture816Duration(),selectedEq:state.selectedEq,intent:$('#scanSheet')?.dataset.captureIntent||''}}
function capture816RenderDraft(){const x=capture816DraftSnapshot(),mode=x.mode;$$('#v816CaptureMode [data-v816-mode]').forEach(b=>b.setAttribute('aria-selected',b.dataset.v816Mode===mode?'true':'false'));const meta=$('#v816CaptureMeta');if(meta)meta.textContent=\`照片 ${'${x.photos.length}'}/${'${x.maxPhotos}'}${'${x.video||x.recording?` · 视频 ${x.recording?`${Math.floor(x.durationMs/1000)}s`:1+\'段\'}`:``}'}\`;const sh=$('#v816Shutter');if(sh){sh.dataset.recording=x.recording?'1':'0';sh.disabled=!!x.busy;sh.style.setProperty('--v816-record',String(Math.min(1,x.durationMs/x.maxVideoMs)))}const st=$('#v816ShutterText'),hint=$('#v816CaptureHint'),limit=$('#v816CaptureLimit');if(st)st.textContent=mode==='video'?(x.recording?'停止':'录制'):mode==='scan'?'扫描':'拍照';if(hint)hint.textContent=mode==='video'?(x.recording?'正在录制':'短视频证据'):mode==='scan'?'连续取样':'可连续拍摄';if(limit)limit.textContent=mode==='video'?\`最长 60 秒${'${x.recording?` · ${Math.floor(x.durationMs/1000)}s`:``}'}\`:mode==='scan'?\`按默认 ${'${String(window.__AXIS_CAPTURE_PREF__?.get?.()||state.prefs.scanSeconds||3)}'} 秒\`:'最多 12 张';const done=$('#v816CaptureDone');if(done)done.disabled=!x.recording&&!x.photos.length&&!x.video;const rail=$('#v816DraftRail');if(rail){const photos=x.photos.map((p,i)=>\`<div class="v816DraftItem"><button type="button" data-v816-cover="\${i}" aria-label="\${i?'设为封面':'当前封面'}"><img src="\${p.url}" alt="现场照片 \${i+1}">\${i===0?'<em>封面</em>':''}</button><button type="button" class="v816Remove" data-v816-remove="\${i}" aria-label="删除照片">×</button></div>\`).join(''),video=x.video||x.recording?\`<div class="v816DraftItem v816DraftVideo"><div><b>\${x.recording?'REC':Math.max(1,Math.round(x.durationMs/1000))+'s'}</b><span>视频</span></div>\${x.recording?'':`<button type="button" class="v816Remove" data-v816-remove-video="1" aria-label="删除视频">×</button>`}</div>\`:'';rail.innerHTML=photos+video||(x.recording?'':'<div class="v816DraftEmpty">拍下的影像会留在这里，完成前都可以调整</div>')}}if(x.recording)setText('#scanState',\`REC · ${'${Math.min(60,Math.floor(x.durationMs/1000))}'}s\`);else if(!capture816Busy)setText('#scanState','就绪')}
function capture816Notify(){capture816RenderDraft();try{D.dispatchEvent(new CustomEvent('axis:capture-draft',{detail:capture816DraftSnapshot()}))}catch{}}
function capture816StartTick(){clearInterval(capture816TickTimer);capture816TickTimer=setInterval(capture816RenderDraft,200);capture816RenderDraft()}
function capture816StopTick(){clearInterval(capture816TickTimer);capture816TickTimer=null;capture816RenderDraft()}
function capture816SetMode(mode){mode=String(mode||'photo');if(!['photo','scan','video'].includes(mode))mode='photo';capture816UiMode=mode;if(mode==='photo')captureMode='photo';else if(mode==='scan'){const pref=String(window.__AXIS_CAPTURE_PREF__?.get?.()||state.prefs.scanSeconds||3);captureMode=['3','5'].includes(pref)?pref:'3'}else{captureMode='video';state.forceClip=true}capture816RenderDraft();return capture816UiMode}
function capture816Open(mode,quickMedia=false){const raw=String(mode||'photo');capture816UiMode=quickMedia&&raw!=='photo'?'video':['3','5'].includes(raw)?'scan':raw==='video'?'video':'photo';if(capture816UiMode==='video')state.forceClip=true;const eq=eqById(state.selectedEq),ctx=$('#v816CaptureContext');if(ctx)ctx.textContent=quickMedia&&eq?\`为 ${'${eq.name}'} 添加影像\`:'现场证据';capture816RenderDraft()}
async function capture816TakePhoto(){if(capture816Busy)return false;if(state.frames.length>=CAPTURE816_PHOTO_MAX){toast('本次最多 12 张照片');return false}if(!state.stream){$('#photoInput')?.click();return false}capture816Busy=true;capture816RenderDraft();try{state.frames.push(await frameFromVideo());capture816Notify();return true}catch(e){console.warn(e);toast('拍摄失败');return false}finally{capture816Busy=false;capture816RenderDraft()}}
async function capture816RunScan(){if(capture816Busy)return false;if(!state.stream){$('#photoInput')?.click();return false}const sec=Math.max(3,Math.min(5,Number(String(window.__AXIS_CAPTURE_PREF__?.get?.()||state.prefs.scanSeconds||3))||3)),want=sec===5?5:4,room=CAPTURE816_PHOTO_MAX-state.frames.length,count=Math.min(want,room);if(count<=0){toast('本次最多 12 张照片');return false}capture816Busy=true;capture816RenderDraft();try{const step=count>1?sec*1000/(count-1):0;for(let i=0;i<count;i++){setText('#scanState',\`取样 ${'${i+1}'}/${'${count}'}\`);state.frames.push(await frameFromVideo());capture816Notify();if(i<count-1)await sleep(step)}return true}catch(e){console.warn(e);toast('扫描失败');return false}finally{capture816Busy=false;capture816RenderDraft()}}
function capture816RemovePhoto(i){i=Number(i);const f=state.frames[i];if(!f)return false;try{URL.revokeObjectURL(f.url)}catch{}state.frames.splice(i,1);capture816Notify();return true}
function capture816SetCover(i){i=Number(i);if(i<=0||i>=state.frames.length)return false;const [f]=state.frames.splice(i,1);state.frames.unshift(f);capture816Notify();return true}
function capture816RemoveVideo(){if(capture816Recorder?.state==='recording')return false;if(state.clip?.url)try{URL.revokeObjectURL(state.clip.url)}catch{}state.clip=null;capture816Notify();return true}
async function capture816StartVideo(){if(capture816Recorder?.state==='recording')return true;if(!state.stream||!window.MediaRecorder){toast('当前设备暂不支持视频拍摄');return false}capture816UiMode='video';captureMode='video';state.forceClip=true;capture816Chunks=[];capture816Discard=false;capture816StartedAt=Date.now();let rec=null;const mt=mediaMime(),base=mt?{mimeType:mt}:{};try{rec=new MediaRecorder(state.stream,{...base,videoBitsPerSecond:2500000})}catch{try{rec=new MediaRecorder(state.stream,base)}catch(e){console.warn(e);toast('当前设备暂不支持视频拍摄');return false}}capture816Recorder=rec;rec.ondataavailable=e=>{if(e.data?.size)capture816Chunks.push(e.data)};rec.onstop=()=>{clearTimeout(capture816StopTimer);capture816StopTimer=null;const discard=capture816Discard,dur=Math.min(CAPTURE816_VIDEO_MAX_MS,Math.max(0,Date.now()-capture816StartedAt)),chunks=capture816Chunks.slice(),type=rec.mimeType||chunks[0]?.type||'video/webm';capture816Recorder=null;capture816Chunks=[];capture816Discard=false;if(!discard&&chunks.length){const blob=new Blob(chunks,{type});if(state.clip?.url)try{URL.revokeObjectURL(state.clip.url)}catch{}state.clip={blob,url:URL.createObjectURL(blob),type:blob.type,durationMs:dur}}capture816StopTick();const waiters=capture816StopWaiters.splice(0);waiters.forEach(r=>r());capture816Notify()};try{rec.start(1000)}catch(e){capture816Recorder=null;console.warn(e);toast('视频启动失败');return false}capture816StopTimer=setTimeout(()=>{toast('已录满 60 秒');capture816StopVideo(false)},CAPTURE816_VIDEO_MAX_MS);capture816StartTick();capture816Notify();return true}
function capture816StopVideo(discard=false){const rec=capture816Recorder;if(!rec||rec.state==='inactive'){capture816StopTick();return Promise.resolve()}if(discard)capture816Discard=true;return new Promise(resolve=>{capture816StopWaiters.push(resolve);try{if(rec.state==='recording')rec.requestData?.();rec.stop()}catch{capture816Recorder=null;capture816StopTick();capture816StopWaiters.splice(0).forEach(r=>r());resolve()}})}
function capture816AbortVideo(discard=true){if(capture816Recorder?.state==='recording')capture816StopVideo(discard);else capture816StopTick()}
async function capture816ToggleVideo(){if(capture816Recorder?.state==='recording'){await capture816StopVideo(false);return false}return capture816StartVideo()}
async function capture816Finish(){if(capture816Recorder?.state==='recording')await capture816StopVideo(false);if(!state.frames.length&&!state.clip){toast('先留下照片或视频');return false}reviewFrames();return true}
function capture816Bind(){const modes=$('#v816CaptureMode');if(!modes||modes.dataset.bound==='1')return;modes.dataset.bound='1';modes.addEventListener('click',async e=>{const b=e.target.closest('[data-v816-mode]');if(!b)return;if(capture816Recorder?.state==='recording'&&b.dataset.v816Mode!=='video')await capture816StopVideo(false);capture816SetMode(b.dataset.v816Mode)});$('#v816Shutter')?.addEventListener('click',async()=>{if(capture816UiMode==='video')await capture816ToggleVideo();else if(capture816UiMode==='scan')await capture816RunScan();else await capture816TakePhoto()});$('#v816CaptureDone')?.addEventListener('click',capture816Finish);$('#v816DraftRail')?.addEventListener('click',e=>{const rm=e.target.closest('[data-v816-remove]');if(rm){capture816RemovePhoto(rm.dataset.v816Remove);return}if(e.target.closest('[data-v816-remove-video]')){capture816RemoveVideo();return}const cover=e.target.closest('[data-v816-cover]');if(cover)capture816SetCover(cover.dataset.v816Cover)});D.addEventListener('visibilitychange',()=>{if(D.hidden&&capture816Recorder?.state==='recording')capture816StopVideo(false)},{passive:true});window.addEventListener('pagehide',()=>capture816AbortVideo(true),{passive:true});capture816RenderDraft()}
`;
 app=once(app,'function setVal(id,v){',captureFns+'function setVal(id,v){','Capture Field canonical API');
 app=once(app,"async function captureNow(){if(captureMode==='photo'){if(state.stream){try{state.frames=[await frameFromVideo()];state.clip=null;reviewFrames();return}catch{}}$('#photoInput').click();return}await liveScan(Number(captureMode)||state.prefs.scanSeconds||3)}","async function captureNow(){if(captureMode==='video')return capture816ToggleVideo();if(captureMode==='photo')return capture816TakePhoto();return capture816RunScan()}",'legacy shutter delegates to 8.16');
 app=once(app,"function resetScan(preserveSelection=false){","function resetScan(preserveSelection=false){capture816AbortVideo(true);",'reset aborts recorder');
 app=once(app,"$('#film').innerHTML=state.frames.map(f=>`<img src=\"${f.url}\" alt=\"扫描画面\">`).join('');","$('#film').innerHTML=(state.clip?.url?`<video src=\"${state.clip.url}\" controls playsinline muted preload=\"metadata\" aria-label=\"本次视频证据\"></video>`:'')+state.frames.map(f=>`<img src=\"${f.url}\" alt=\"扫描画面\">`).join('');",'review video preview');
 app=regexOnce(app,/\$\('#photoInput'\)\.onchange=async e=>\{[\s\S]*?e\.target\.value=''\};/,"$('#photoInput').onchange=async e=>{const files=Array.from(e.target.files||[]).filter(f=>String(f.type||'').startsWith('image/')).slice(0,Math.max(0,CAPTURE816_PHOTO_MAX-state.frames.length));if(!files.length){e.target.value='';return}capture816Busy=true;capture816RenderDraft();try{for(const f of files)state.frames.push(await frameFromFile(f));capture816Notify()}catch(err){console.warn(err);toast('照片读取失败')}finally{capture816Busy=false;capture816RenderDraft();e.target.value=''}};",'multi-photo file bridge');
 app=regexOnce(app,/function openCanonicalCamera\(mode,id=null,quickMedia=false\)\{[\s\S]*?startCamera\(\);return true\}/,`function openCanonicalCamera(mode,id=null,quickMedia=false){const e=id?eqById(id):null;if(id&&!e)return false;resetScan(!!id);if(e){state.selectedEq=id;selectEq(id,false)}state.forceClip=!!quickMedia&&String(mode)!=='photo';captureMode=String(mode||state.prefs.scanSeconds||3);const s=$('#scanSheet');if(!s)return false;s.classList.add('show');s.classList.remove('v8-quick');s.classList.toggle('v882-quick-media',!!quickMedia);s.dataset.captureOwner='canonical';s.dataset.captureIntent=quickMedia?'quick-media':'record';$('#captureStage')?.classList.remove('hidden');$('#reviewStage')?.classList.add('hidden');const h=s.querySelector('.sheetHead>b');if(h)h.textContent='拍摄记录';setText('#captureNow',captureMode==='photo'?'拍照':captureMode==='video'?'录制视频':\`开始扫描 ${'${captureMode}'} 秒\`);$$('#captureModes button').forEach(b=>b.classList.toggle('active',b.dataset.mode===captureMode));startCamera();capture816Open(captureMode,quickMedia);return true}`,'canonical camera opens 8.16 field');
 app=once(app,'load();buildChoices();bind();render();aiHealth();','load();buildChoices();bind();capture816Bind();render();aiHealth();','Capture Field binding');
 app=regexOnce(app,/window\.__AXIS_CAPTURE__=\{beginQuickMedia,openCanonicalCamera,prepareQuick:id=>\{const e=eqById\(id\);if\(!e\)return false;state\.selectedEq=id;selectEq\(id,false\);return true\},snapshot:\(\)=>\(\{mode:captureMode,selectedEq:state\.selectedEq,owner:\$\('#scanSheet'\)\?\.dataset\.captureOwner\|\|'',intent:\$\('#scanSheet'\)\?\.dataset\.captureIntent\|\|''\}\)\};/,"window.__AXIS_CAPTURE__={beginQuickMedia,openCanonicalCamera,prepareQuick:id=>{const e=eqById(id);if(!e)return false;state.selectedEq=id;selectEq(id,false);return true},snapshot:()=>({mode:captureMode,selectedEq:state.selectedEq,owner:$('#scanSheet')?.dataset.captureOwner||'',intent:$('#scanSheet')?.dataset.captureIntent||''}),draft:capture816DraftSnapshot,setMode:capture816SetMode,takePhoto:capture816TakePhoto,scan:capture816RunScan,startVideo:capture816StartVideo,stopVideo:capture816StopVideo,finish:capture816Finish,removePhoto:capture816RemovePhoto,setCover:capture816SetCover,removeVideo:capture816RemoveVideo,maxPhotos:CAPTURE816_PHOTO_MAX,maxVideoMs:CAPTURE816_VIDEO_MAX_MS};",'extended canonical capture bridge');
 const end=app.lastIndexOf('})();');if(end<0)fail('app IIFE end missing');
 const marker="\ntry{window.__AXIS_816_CAPTURE_FIELD__={version:'8.16',owner:'app.js',surface:'v816-capture-field',multiPhoto:true,maxPhotos:12,video:true,maxVideoSeconds:60,audio:false,oneRecorder:true,persistenceOwner:'app.js',mediaStore:'axis_v42_media',newStorage:false}}catch{}\n";
 app=app.slice(0,end)+marker+app.slice(end);
 for(const needle of ['CAPTURE816_PHOTO_MAX=12','CAPTURE816_VIDEO_MAX_MS=60000','capture816TakePhoto','capture816StartVideo','capture816RunScan','capture816SetCover','__AXIS_816_CAPTURE_FIELD__'])if(!app.includes(needle))fail(`capture implementation missing ${needle}`);
 try{new Function(app)}catch(e){fail(`app syntax ${e.message}`)}
 write(APP,app);
}

/* 8.15.1 stable swaps stay intact. 8.16 only replaces endpoint-only comparison with
   a two-point lens over any two real photo-bearing Encounters. */
{
 let media=read(MEDIA);
 if(!media.includes('__AXIS_8151_MEDIA_SWAP__')||!media.includes('retainPreviousUntilReady:true'))fail('8.15.1 stable Media Evidence must converge first');
 media=once(media,"let currentKey='',renderEpoch=0,compareMode=false,selectedEncounter=-1,selectedRef='',objectUrls=new Set();","let currentKey='',renderEpoch=0,compareMode=false,selectedEncounter=-1,selectedRef='',compareLeft=-1,compareRight=-1,compareSide='right',objectUrls=new Set();",'comparative state');
 media=regexOnce(media,/function resolveBundle\(key\)\{[\s\S]*?\}\nfunction installStyle/,`function resolveBundle(key){key=String(key||'').trim();const base=evolution()?.resolve?.(key);if(!key||!base)return null;const encounters=(base.encounters||[]).map(normalizeEncounter),visual=encounters.filter(x=>x.media.length),photoEncounters=visual.filter(x=>x.photos.length),photos=visual.reduce((n,x)=>n+x.photos.length,0),videos=visual.reduce((n,x)=>n+x.videos.length,0);return{id:key,name:base.name||'Evolution',encounterCount:Number(base.encounterCount)||encounters.length,evidenceEncounterCount:visual.length,evidenceAssetCount:photos+videos,photoCount:photos,videoCount:videos,encounters,visualEncounters:visual,photoEncounters,earliestVisual:visual[0]||null,latestVisual:visual.at(-1)||null,compareAvailable:photoEncounters.length>1}}\nfunction installStyle`,'photo-bearing compare bundle');
 media=once(media,"function pickRef(enc){if(!enc)return'';if(selectedRef&&enc.media.includes(selectedRef))return selectedRef;return enc.photos[0]||enc.videos[0]||enc.media[0]||''}","function pickRef(enc){if(!enc)return'';if(selectedRef&&enc.media.includes(selectedRef))return selectedRef;return enc.photos[0]||enc.videos[0]||enc.media[0]||''}\nfunction compareCandidates(bundle){return bundle?.photoEncounters||[]}\nfunction comparePair(bundle){const c=compareCandidates(bundle);if(c.length<2)return[null,null];let a=c.find(x=>x.index===compareLeft),b=c.find(x=>x.index===compareRight);if(!a)a=c[0];if(!b||b.index===a.index)b=c.at(-1);if(b.index===a.index)b=c.find(x=>x.index!==a.index)||null;if(!b)return[null,null];compareLeft=a.index;compareRight=b.index;return[a,b]}\nfunction comparePreset(bundle,preset){const c=compareCandidates(bundle);if(c.length<2)return;if(preset==='recent'){compareLeft=c.at(-2).index;compareRight=c.at(-1).index}else if(preset==='adjacent'){const n=c.findIndex(x=>x.index===selectedEncounter),i=n>0?n:c.length-1;compareLeft=c[Math.max(0,i-1)].index;compareRight=c[i].index}else{compareLeft=c[0].index;compareRight=c.at(-1).index}compareSide='right'}\nfunction compareSelect(bundle,index){const c=compareCandidates(bundle),x=c.find(v=>v.index===Number(index));if(!x)return;if(compareLeft<0||compareRight<0)comparePreset(bundle,'ends');if(compareSide==='left'){if(x.index===compareRight){const old=compareLeft;compareLeft=x.index;compareRight=old}else compareLeft=x.index}else{if(x.index===compareLeft){const old=compareRight;compareRight=x.index;compareLeft=old}else compareRight=x.index}}\nfunction compareDays(a,b){return Math.max(0,Math.round(Math.abs((Number(b?.time)||0)-(Number(a?.time)||0))/86400000))}", 'comparative helpers');
 const compare=`async function renderCompare(section,bundle,epoch){
 const stage=$('.v815Stage',section),[a,b]=comparePair(bundle);if(!stage||!a||!b||!bundle.compareAvailable)return;
 section.dataset.loading='1';
 const [ua,ub]=await Promise.all([objectUrl(a.photos[0]),objectUrl(b.photos[0])]);if(epoch!==renderEpoch){dropObjectUrl(ua);dropObjectUrl(ub);return}
 if(!ua||!ub){dropObjectUrl(ua);dropObjectUrl(ub);compareMode=false;const enc=pickEncounter(bundle),ref=pickRef(enc);return renderVisual(section,bundle,enc,ref,epoch)}
 await Promise.all([warmMedia(ua,a.photos[0]),warmMedia(ub,b.photos[0])]);if(epoch!==renderEpoch){dropObjectUrl(ua);dropObjectUrl(ub);return}
 const days=compareDays(a,b),tools=\`<div class="v816CompareTools"><button type="button" data-v816-compare-side="left" aria-pressed="\${compareSide==='left'?'true':'false'}">左 · 第\${a.index}次</button><button type="button" data-v816-compare-side="right" aria-pressed="\${compareSide==='right'?'true':'false'}">右 · 第\${b.index}次</button><button type="button" data-v816-compare-preset="ends">首尾</button><button type="button" data-v816-compare-preset="recent">最近两次</button><button type="button" data-v816-compare-preset="adjacent">相邻</button><span class="v816CompareMeta">\${days?\`跨\${days}天\`:'同日'}</span></div>\`;
 const next=tools+\`<div class="v815Compare"><figure><img src="\${ua.url}" alt="第\${a.index}次影像"><figcaption>第\${a.index}次<span>\${esc(fmtDate(a.time))} · \${esc(a.summary||'')}</span></figcaption></figure><figure><img src="\${ub.url}" alt="第\${b.index}次影像"><figcaption>第\${b.index}次<span>\${esc(fmtDate(b.time))} · \${esc(b.summary||'')}</span></figcaption></figure></div>\`;
 stage.innerHTML=next;revokeExcept([ua.url,ub.url]);section.dataset.loading='0';$$('.v815Rail [data-v815-encounter]',section).forEach(x=>x.dataset.v816Pair=[a.index,b.index].includes(Number(x.dataset.v815Encounter))?'1':'0')
}`;
 media=regexOnce(media,/async function renderCompare\(section,bundle,epoch\)\{[\s\S]*?\}\nasync function renderEvidence/,compare+'\nasync function renderEvidence','arbitrary pair renderer');
 media=once(media,'>首尾对照</button>','>对照</button>','compare entry label');
 media=once(media,"function closeEvidence(){renderEpoch++;currentKey='';compareMode=false;selectedEncounter=-1;selectedRef='';revokeUrls();$('#v815Evidence')?.remove()}","function closeEvidence(){renderEpoch++;currentKey='';compareMode=false;selectedEncounter=-1;selectedRef='';compareLeft=-1;compareRight=-1;compareSide='right';revokeUrls();$('#v815Evidence')?.remove()}",'compare close reset');
 media=once(media,"const encBtn=e.target.closest?.('[data-v815-encounter]');if(encBtn){selectedEncounter=Number(encBtn.dataset.v815Encounter)||-1;selectedRef='';compareMode=false;renderEvidence(currentKey);return}","const sideBtn=e.target.closest?.('[data-v816-compare-side]');if(sideBtn){compareSide=sideBtn.dataset.v816CompareSide==='left'?'left':'right';renderEvidence(currentKey);return}const presetBtn=e.target.closest?.('[data-v816-compare-preset]');if(presetBtn){comparePreset(resolveBundle(currentKey),presetBtn.dataset.v816ComparePreset);compareMode=true;renderEvidence(currentKey);return}const encBtn=e.target.closest?.('[data-v815-encounter]');if(encBtn){const idx=Number(encBtn.dataset.v815Encounter)||-1;selectedEncounter=idx;selectedRef='';if(compareMode){compareSelect(resolveBundle(currentKey),idx)}else compareMode=false;renderEvidence(currentKey);return}",'compare rail interaction');
 media=once(media,"if(compare){compareMode=!compareMode;compare.setAttribute('aria-pressed',compareMode?'true':'false');renderEvidence(currentKey);return}","if(compare){compareMode=!compareMode;if(compareMode&&compareLeft<0)comparePreset(resolveBundle(currentKey),'ends');compare.setAttribute('aria-pressed',compareMode?'true':'false');renderEvidence(currentKey);return}",'compare toggle initialization');
 media=once(media,"window.__AXIS_8151_MEDIA_SWAP__={version:'8.15.1'","window.__AXIS_816_COMPARATIVE_EVIDENCE__={version:'8.16',owner:'v815-media-evidence',arbitraryPair:true,presets:['ends','recent','adjacent'],railSelectsActiveSide:true,factualOnly:true,stableSwapInherited:true,replay:false};window.__AXIS_8151_MEDIA_SWAP__={version:'8.15.1'",'8.16 comparative marker');
 for(const needle of ['photoEncounters','compareSelect','data-v816-compare-side','data-v816-compare-preset','__AXIS_816_COMPARATIVE_EVIDENCE__','retainPreviousUntilReady:true','warmBeforeCommit:true'])if(!media.includes(needle))fail(`comparative evidence missing ${needle}`);
 for(const forbidden of ['localStorage.setItem','sessionStorage.setItem','indexedDB.open','fetch(','XMLHttpRequest'])if(media.includes(forbidden))fail(`comparative evidence acquired forbidden owner ${forbidden}`);
 try{new Function(media)}catch(e){fail(`Media Evidence syntax ${e.message}`)}
 write(MEDIA,media);
}

console.log('[AXIS 8.16 Capture + Evidence convergence] PASS · one canonical Capture Field · 12-photo draft · one <=60s silent video · existing media persistence · arbitrary factual two-point comparison · 8.15.1 stable swap inherited');
