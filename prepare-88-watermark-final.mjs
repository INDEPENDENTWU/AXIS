import fs from 'node:fs';

const fail=m=>{throw new Error(`AXIS 8.8 watermark final: ${m}`)};
const read=f=>{if(!fs.existsSync(f))fail(`missing ${f}`);return fs.readFileSync(f,'utf8')};
const write=(f,s)=>fs.writeFileSync(f,s);
const once=(src,from,to,label)=>{const n=src.split(from).length-1;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(from,to)};
const regexOnce=(src,re,to,label)=>{const flags=re.flags.includes('g')?re.flags:re.flags+'g',n=(src.match(new RegExp(re.source,flags))||[]).length;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(re,()=>to)};
const syntax=(src,label)=>{try{new Function(src)}catch(e){fail(`${label} syntax ${e.message}`)}};

/* v85 remains the sole visible switch writer. Time becomes the fourth explicit field. */
{
  const FILE='v85-runtime.js';let src=read(FILE);
  src=once(src,"function wmPrefs(){const p=prefs();return{name:p.v85WmName!==false,data:p.v85WmData!==false,location:!!p.v85WmLocation,pos:p.v85WmPos||readCore().prefs?.watermark?.pos||'bl'}}","function wmPrefs(){const p=prefs();return{name:p.v85WmName!==false,data:p.v85WmData!==false,location:!!p.v85WmLocation,time:p.v85WmTime!==false,pos:p.v85WmPos||readCore().prefs?.watermark?.pos||'bl'}}",'v85 preference shape');
  src=once(src,'<div class="v85WmRow"><span>位置</span><button class="switch" id="v85WmLocation" role="switch"><i></i></button></div>','<div class="v85WmRow"><span>位置</span><button class="switch" id="v85WmLocation" role="switch"><i></i></button></div><div class="v85WmRow"><span>时间</span><button class="switch" id="v85WmTime" role="switch"><i></i></button></div>','v85 time row');
  src=once(src,"$('#v85WmData').onclick=()=>setWm('v85WmData',!wmPrefs().data);$('#v85WmLocation')","$('#v85WmData').onclick=()=>setWm('v85WmData',!wmPrefs().data);$('#v85WmTime').onclick=()=>setWm('v85WmTime',!wmPrefs().time);$('#v85WmLocation')",'v85 time writer');
  const render="function renderWm(){const p=wmPrefs(),g=latestGeo||prefs().v85LastGeo;for(const [id,on] of [['v85WmName',p.name],['v85WmData',p.data],['v85WmLocation',p.location],['v85WmTime',p.time]])$('#'+id)?.setAttribute('aria-checked',String(on));const b=$('#v85WmPreview');if(!b)return;b.style.left=p.pos.endsWith('r')?'auto':'18px';b.style.right=p.pos.endsWith('r')?'18px':'auto';b.style.top=p.pos.startsWith('t')?'18px':'auto';b.style.bottom=p.pos.startsWith('t')?'auto':'18px';$('#v85WmNamePreview').style.display=p.name?'block':'none';$('#v85WmDataPreview').style.display=p.data?'block':'none';const d=new Date(),time=`${d.getFullYear()}.${pad(d.getMonth()+1)}.${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`,meta=p.time?time:'';$('#v85WmMetaPreview').textContent=meta;$('#v85WmMetaPreview').style.display=meta?'block':'none'}";
  src=regexOnce(src,/function renderWm\(\)\{[\s\S]*?\}\nfunction wmData/,render+'\nfunction wmData','v85 preview contract');
  src=regexOnce(src,/const d=new Date\(ts\|\|now\(\)\),meta=[\s\S]*?;c\.save\(\);/,"const d=new Date(ts||now()),time=`${d.getFullYear()}.${pad(d.getMonth()+1)}.${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`,meta=p.time?time:'';c.save();",'v85 legacy time contract');
  syntax(src,FILE);write(FILE,src);
}

/* v876 is compatibility input after prepare-88-convergence; coordinates are already private there. */
{
  const FILE='v876-runtime.js';let src=read(FILE);
  src=once(src,"return{name:p.v85WmName!==false,data:p.v85WmData!==false,location:!!p.v85WmLocation,pos:p.v85WmPos||w.pos||'bl',photoMode:w.photoMode||'wm',opacity:Math.max(4,Math.min(32,Number(p.v876WmOpacity)||15)),...locationPrefs()}","return{name:p.v85WmName!==false,data:p.v85WmData!==false,location:!!p.v85WmLocation,time:p.v85WmTime!==false,pos:p.v85WmPos||w.pos||'bl',photoMode:w.photoMode||'wm',opacity:Math.max(4,Math.min(32,Number(p.v876WmOpacity)||15)),...locationPrefs()}",'v876 preference shape');
  const timeWrites=(src.match(/c\.fillText\(time,x,yy\);/g)||[]).length;if(timeWrites!==1)fail(`v876 time writer expected once, found ${timeWrites}`);src=src.replace('c.fillText(time,x,yy);','if(p.time)c.fillText(time,x,yy);');
  syntax(src,FILE);write(FILE,src);
}

/* v8710 is the final preview/photo owner. The earlier product compiler already owns the OSM resolver. */
{
  const FILE='v8710-watermark.js';let src=read(FILE);
  src=regexOnce(src,/return\{lang:p\.v8710WmLang\|\|'auto',resolvedLang:L,geo:p\.v85LastGeo\|\|null,place:manual\|\|String\(cache\[L\]\|\|p\.v8710PlaceName\|\|p\.v876LocationNameAuto\|\|''\)\.trim\(\),manual,location:p\.v85WmLocation!==false,opacity:/,"return{lang:p.v8710WmLang||'auto',resolvedLang:L,geo:p.v85LastGeo||null,place:manual||String(cache[L]||p.v8710PlaceName||p.v876LocationNameAuto||'').trim(),manual,name:p.v85WmName!==false,data:p.v85WmData!==false,location:p.v85WmLocation!==false,time:p.v85WmTime!==false,opacity:",'v8710 preference fields');
  src=once(src,'.v8710LocationMeta{margin-top:12px;color:var(--dim);font-size:var(--axis-ui,15px);line-height:1.45}','.v8710LocationMeta{margin-top:12px;color:var(--dim);font-size:var(--axis-ui,15px);line-height:1.45}#v876Coord{display:none!important}','coordinate row retirement');

  src=regexOnce(src,/async function reverse\(g,L=selectedLang\(\)\)\{if\(!g\)return'';const id=\+\+placeReq,m0=meta\(\),cached=m0\.prefs\.v8712PlaceResolve;if\(cached\?\.name&&Date\.now\(\)-Number\(cached\.ts\|\|0\)<21600000&&distanceMeters\(g,cached\)<=80\)return cached\.name;/,"async function reverse(g,L=selectedLang(),force=false){if(!g)return'';const id=++placeReq,m0=meta(),cached=m0.prefs.v8712PlaceResolve;if(!force&&cached?.name&&Date.now()-Number(cached.ts||0)<21600000&&distanceMeters(g,cached)<=80)return cached.name;",'forceable place resolver');
  src=once(src,"function freshGeo(){const g=pref().geo;if(!pref().location||!navigator.geolocation)return Promise.resolve(g);if(g&&Date.now()-(g.ts||0)<25000&&Number(g.acc)<=30)return Promise.resolve(g);","function freshGeo(force=false){const g=pref().geo;if((!force&&!pref().location)||!navigator.geolocation)return Promise.resolve(g);if(!force&&g&&Date.now()-(g.ts||0)<25000&&Number(g.acc)<=30)return Promise.resolve(g);",'forceable geolocation');
  src=once(src,"async function refreshPlace(){const g=await freshGeo();await reverse(g,selectedLang());render();return g}","async function refreshPlace(force=false){const g=await freshGeo(force);await reverse(g,selectedLang(),force);render();return g}",'forceable precise refresh');
  src=once(src,"if(e.target.closest('#v876Locate'))setTimeout(()=>refreshPlace(),500)","if(e.target.closest('#v876Locate'))setTimeout(()=>refreshPlace(true),0)",'explicit locate refresh');

  const helpers="function currentName(){const n=$('#equipmentName')?.textContent.trim()||'TRAINING';return selectedLang()==='en'?englishName(n).toUpperCase():n}\nfunction previewData(){const rows=$$('#v8Sets .v8SetRow');if(rows.length){const vals=rows.map(r=>$$('span>b',r).map(x=>x.textContent.trim())),weights=[...new Set(vals.map(x=>x[0]).filter(Boolean))],reps=vals.map(x=>x[1]).filter(Boolean);if(weights.length===1&&reps.length&&new Set(reps).size===1)return `${weights[0]} kg · ${reps[0]} 次 × ${rows.length}`;return `${rows.length} 组 · 分组记录`}if(!$('#strengthFields')?.classList.contains('hidden')){const w=$('#weight')?.value||'—',r=$('#reps')?.value||'—';return `${w} kg · ${r} 次`}if(!$('#cardioFields')?.classList.contains('hidden'))return `${$('#duration')?.value||'—'} 分钟`;return''}\nfunction timeText(ts=Date.now()){const d=new Date(ts);return `${d.getFullYear()}.${pad(d.getMonth()+1)}.${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`}";
  src=regexOnce(src,/function currentName\(\)\{[^\n]*\}\nfunction coord\(g\)\{[^\n]*\}/,helpers,'preview helpers');
  src=once(src,'<div class="v8710WmPreview" id="v8710WmPreview"><div class="v8710WmRail"><b>AXIS / RECORD</b><span id="v8710WmName"></span><span id="v8710WmLoc"></span></div></div>','<div class="v8710WmPreview" id="v8710WmPreview"><div class="v8710WmRail"><b>AXIS / RECORD</b><span id="v8710WmName"></span><span id="v8710WmData"></span><span id="v8710WmLoc"></span><span id="v8710WmTime"></span></div></div>','final preview rows');
  const render="function render(){sync();const p=pref(),name=currentName(),data=previewData(),time=timeText();if($('#v8710LocationMeta'))$('#v8710LocationMeta').textContent=p.place||'尚未获取位置';if($('#v876LocationName'))$('#v876LocationName').textContent=p.place||'未获取';const set=(id,on,text)=>{const el=$(id);if(!el)return;el.textContent=text||'';el.style.display=on&&text?'block':'none'};set('#v8710WmName',p.name,name);set('#v8710WmData',p.data,data);set('#v8710WmLoc',p.location,p.place);set('#v8710WmTime',p.time,time);const host=$('#v876Location');if(host){let c=$('#v8712PlaceCredit',host);if(!c){c=D.createElement('small');c.id='v8712PlaceCredit';c.className='v8712PlaceCredit';host.appendChild(c)}c.textContent=p.source==='osm'&&!p.manual?'地名 © OpenStreetMap contributors':''}}";
  src=regexOnce(src,/function render\(\)\{[\s\S]*?\}\nfunction dbGet/,render+'\nfunction dbGet','final preview renderer');

  const stamp="async function stamp(blob,e){const url=URL.createObjectURL(blob),img=new Image();await new Promise((ok,no)=>{img.onload=ok;img.onerror=no;img.src=url});const cv=D.createElement('canvas');cv.width=img.naturalWidth;cv.height=img.naturalHeight;const c=cv.getContext('2d',{alpha:false});c.imageSmoothingEnabled=true;c.imageSmoothingQuality='high';c.drawImage(img,0,0,cv.width,cv.height);URL.revokeObjectURL(url);const p=pref(),L=p.resolvedLang,W=cv.width,H=cv.height,base=Math.max(20,Math.round(W*.0215)),big=Math.max(25,Math.round(W*.027)),pd=Math.max(22,Math.round(W*.03));c.save();c.globalAlpha=Math.max(.10,p.opacity/100);c.fillStyle='#737cff';c.textAlign='center';c.textBaseline='middle';c.font=`800 ${Math.max(74,Math.round(W*.12))}px -apple-system,BlinkMacSystemFont,Arial`;c.fillText('A X I S',W/2,H*.48);c.globalAlpha=1;c.fillStyle='rgba(115,124,255,.72)';c.fillRect(W*.12,H*.555,W*.76,Math.max(2,Math.round(W*.0025)));const name=L==='en'?englishName(e.name||'TRAINING').toUpperCase():(e.name||'训练'),time=timeText(e.time||Date.now()),data=eventData(e,L),rows=[];if(p.name)rows.push({text:name,size:big,weight:680,color:'#fff'});if(p.data&&data)rows.push({text:data,size:base,weight:580,color:'rgba(255,255,255,.96)'});if(p.location&&p.place)rows.push({text:p.place,size:base,weight:540,color:'rgba(255,255,255,.91)'});if(p.time)rows.push({text:time,size:base,weight:540,color:'rgba(255,255,255,.86)'});const maxText=Math.round(W*.52),lineH=Math.round(base*1.38),boxW=Math.min(Math.round(W*.62),Math.max(Math.round(W*.40),maxText+pd)),boxH=lineH*(1+rows.length)+pd*1.35,[x,y]=panelRect(W,H,p.pos,boxW,boxH,pd),right=p.pos==='tr'||p.pos==='br';c.fillStyle='rgba(5,7,10,.70)';c.fillRect(x,y,boxW,boxH);c.fillStyle='#737cff';c.fillRect(right?x+boxW-Math.max(3,W*.004):x,y,Math.max(3,W*.004),boxH);const tx=right?x+boxW-pd:x+pd;c.textAlign=right?'right':'left';c.textBaseline='top';c.shadowColor='rgba(0,0,0,.75)';c.shadowBlur=Math.max(4,W*.004);c.fillStyle='#fff';c.font=`720 ${base}px -apple-system,BlinkMacSystemFont,'PingFang SC',Arial`;c.fillText('AXIS / RECORD',tx,y+pd*.55);let yy=y+pd*.55+lineH;for(const row of rows){const size=fit(c,row.text,boxW-pd*2,row.size,row.weight,Math.max(13,base-6));c.font=`${row.weight} ${size}px -apple-system,BlinkMacSystemFont,'PingFang SC',Arial`;c.fillStyle=row.color;c.fillText(row.text,tx,yy);yy+=lineH}c.restore();return new Promise(ok=>cv.toBlob(ok,'image/jpeg',.97))}";
  src=regexOnce(src,/async function stamp\(blob,e\)\{[\s\S]*?\}\nasync function newest/,stamp+'\nasync function newest','final photo watermark');
  src=once(src,"old:{opacity:Number(p.v876WmOpacity)||18,pos:p.v85WmPos||'br',location:p.v85WmLocation!==false}","old:{opacity:Number(p.v876WmOpacity)||18,pos:p.v85WmPos||'br',name:p.v85WmName!==false,data:p.v85WmData!==false,location:p.v85WmLocation!==false,time:p.v85WmTime!==false}",'pre-save preference snapshot');
  src=once(src,"m.prefs.v876WmOpacity=4;m.prefs.v85WmPos='bl';m.prefs.v85WmLocation=false;write(META,m);setTimeout(()=>finishStamp(a),0)","m.prefs.v876WmOpacity=4;m.prefs.v85WmPos='bl';m.prefs.v85WmName=false;m.prefs.v85WmData=false;m.prefs.v85WmLocation=false;m.prefs.v85WmTime=false;write(META,m);setTimeout(()=>finishStamp(a),0)",'legacy metadata suppression');
  src=once(src,"m.prefs.v876WmOpacity=a.old.opacity;m.prefs.v85WmPos=a.old.pos;m.prefs.v85WmLocation=a.old.location;write(META,m);render()","m.prefs.v876WmOpacity=a.old.opacity;m.prefs.v85WmPos=a.old.pos;m.prefs.v85WmName=a.old.name;m.prefs.v85WmData=a.old.data;m.prefs.v85WmLocation=a.old.location;m.prefs.v85WmTime=a.old.time;write(META,m);render()",'post-save preference restore');
  src=once(src,"D.addEventListener('input',e=>{if(e.target.id==='v877OpacityRange'||e.target.id==='v876OpacityRange')setTimeout(render,0)},true);","D.addEventListener('input',e=>{if(e.target.id==='v877OpacityRange'||e.target.id==='v876OpacityRange')setTimeout(render,0)},true);D.addEventListener('click',e=>{if(e.target.closest('#v85WmName,#v85WmData,#v85WmLocation,#v85WmTime'))setTimeout(render,0)},false);",'preview switch repaint');
  if(/toFixed\(6\).*±|LAT |LON |纬度 .*经度/.test(src))fail('raw coordinate presentation survived final watermark owner');
  syntax(src,FILE);write(FILE,src);
}

console.log('[AXIS 8.8 watermark] final convergence passed · four switches · forced precise OSM refresh · no raw coordinates');