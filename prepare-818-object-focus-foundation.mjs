import fs from 'node:fs';

const fail=m=>{throw new Error(`[AXIS 8.18 foundation] ${m}`)};
const read=f=>{if(!fs.existsSync(f))fail(`missing ${f}`);return fs.readFileSync(f,'utf8')};
const write=(f,s)=>fs.writeFileSync(f,s);
const once=(src,from,to,label)=>{const n=src.split(from).length-1;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(from,to)};
const regexOnce=(src,re,to,label)=>{const flags=re.flags.includes('g')?re.flags:re.flags+'g',n=(src.match(new RegExp(re.source,flags))||[]).length;if(n!==1)fail(`${label} expected once, found ${n}`);return src.replace(re,()=>to)};
const syntax=(src,label)=>{try{new Function(src)}catch(e){fail(`${label} syntax ${e.message}`)}};

/* --------------------------------------------------------------------------
 * 1. app.js remains the persistence / Capture / route truth owner.
 * ----------------------------------------------------------------------- */
{
 const FILE='app.js';let s=read(FILE);
 const end=s.lastIndexOf('})();');if(end<0)fail('app IIFE end missing');
 const block=String.raw`
/* AXIS 8.18 — Object Truth + Route Truth + Capture Preference foundation. */
const AXIS818_METRICS={
 weight:{key:'weight',label:'重量',type:'number',unit:'kg',step:2.5},
 reps:{key:'reps',label:'次数',type:'number',unit:'次',step:1},
 sets:{key:'sets',label:'组数',type:'number',unit:'组',step:1},
 duration:{key:'duration',label:'时间',type:'duration',unit:'分钟',step:1},
 intensity:{key:'intensity',label:'强度',type:'number',unit:'',step:1},
 distance:{key:'distance',label:'距离',type:'number',unit:'km',step:.1},
 resistance:{key:'resistance',label:'阻力 / 档位',type:'number',unit:'',step:1},
 pace:{key:'pace',label:'速度 / 配速',type:'text',unit:'',step:1},
 hold:{key:'hold',label:'保持时间',type:'duration',unit:'秒',step:5}
};
function axis818CloneMetric(x){return{key:String(x.key||''),label:String(x.label||x.key||''),type:String(x.type||'number'),unit:String(x.unit||''),step:Number(x.step)||1,custom:!!x.custom}}
function axis818DefaultSchema(eq){const type=eq?.type||'strength';return(type==='cardio'?[AXIS818_METRICS.duration,AXIS818_METRICS.intensity]:[AXIS818_METRICS.weight,AXIS818_METRICS.reps,AXIS818_METRICS.sets]).map(axis818CloneMetric)}
function axis818Eq(ref){if(!ref)return null;if(typeof ref==='object'&&ref.id)return ref;const id=String(ref),xs=[...(state.profile?.customEq||[]),...BASE_EQ];return xs.find(x=>x.id===id||x.name===id)||null}
function axis818SchemaForEq(ref){const eq=axis818Eq(ref);if(!eq)return[];const explicit=Array.isArray(eq.metricSchema)?eq.metricSchema.filter(x=>x&&x.key):null;return(explicit&&explicit.length?explicit:axis818DefaultSchema(eq)).map(axis818CloneMetric)}
function axis818SchemaForEvent(e){const snap=Array.isArray(e?.metricSchemaSnapshot)?e.metricSchemaSnapshot.filter(x=>x&&x.key):null;return(snap&&snap.length?snap:axis818SchemaForEq(e?.equipmentId||e?.name)).map(axis818CloneMetric)}
function axis818LegacyValue(e,k){if(k==='weight')return e.weight;if(k==='reps')return e.reps;if(k==='sets')return e.sets;if(k==='duration')return e.duration;if(k==='intensity')return e.intensity;return null}
function axis818EventMetrics(e){const out={...(e?.metrics||{})};for(const m of axis818SchemaForEvent(e)){if(out[m.key]==null){const v=axis818LegacyValue(e,m.key);if(v!=null)out[m.key]=v}}return out}
function axis818MetricText(m,v){if(v==null||v==='')return'—';if(m.type==='duration')return String(v)+' '+(m.unit||'分钟');return String(v)+(m.unit?' '+m.unit:'')}
function axis818EventRows(e){const vals=axis818EventMetrics(e);return axis818SchemaForEvent(e).map(m=>[m.label,axis818MetricText(m,vals[m.key])])}
function axis818ReadMetricInputs(schema){const out={};for(const m of schema){const el=D.querySelector('[data-axis818-metric="'+m.key+'"]');let v=el?.value??'';if(m.type==='number'||m.type==='duration')v=v===''?null:Number(v);out[m.key]=v}return out}
function axis818ApplyLegacy(e,vals){for(const k of ['weight','reps','sets','duration','intensity'])if(vals[k]!=null)e[k]=Number(vals[k]);}
function axis818RenderRecorder(){const eq=eqById(state.selectedEq);if(!eq)return;const custom=(state.profile?.customEq||[]).find(x=>x.id===eq.id),schema=axis818SchemaForEq(eq);let host=$('#axis818MetricRecorder');if(!host){host=D.createElement('div');host.id='axis818MetricRecorder';host.className='axis818MetricRecorder';const anchor=$('#saveScan');anchor?.parentNode?.insertBefore(host,anchor)}if(!host)return;const explicit=!!custom?.metricSchema?.length;host.classList.toggle('show',explicit);if(!explicit)return;$('#strengthFields')?.classList.add('axis818LegacyMetricHidden');$('#cardioFields')?.classList.add('axis818LegacyMetricHidden');host.innerHTML='<div class="axis818MetricHead"><span>本次记录</span><b>'+esc(eq.name)+'</b></div>'+schema.map(m=>'<label class="axis818MetricField"><span>'+esc(m.label)+'</span><div><input data-axis818-metric="'+esc(m.key)+'" '+(m.type==='text'?'':'inputmode="decimal"')+' placeholder="—"><small>'+esc(m.unit||'')+'</small></div></label>').join('');const prev=allEvents().filter(x=>x.equipmentId===eq.id).sort((a,b)=>(b.time||0)-(a.time||0))[0];const pv=prev?axis818EventMetrics(prev):{};for(const m of schema){const el=D.querySelector('[data-axis818-metric="'+m.key+'"]');if(el&&pv[m.key]!=null)el.value=String(pv[m.key])}}
function axis818CaptureEvent(e,eq){const schema=axis818SchemaForEq(eq),vals=axis818ReadMetricInputs(schema);e.metricSchemaSnapshot=schema.map(axis818CloneMetric);e.metrics=vals;axis818ApplyLegacy(e,vals);e.objectTruthVersion='8.18';return e}
window.__AXIS_OBJECT_TRUTH__={version:'8.18',owner:'app.js',schemaForEq:axis818SchemaForEq,schemaForEvent:axis818SchemaForEvent,eventMetrics:axis818EventMetrics,eventRows:axis818EventRows,explicit:eq=>!!axis818Eq(eq)?.metricSchema?.length};

/* Route Truth: returning from background can never reactivate Today controls over another view. */
let axis818Route=(D.querySelector('.view.active')||D.querySelector('.page.active'))?.id||'todayView',axis818RouteLock=false;
function axis818CurrentRoute(){return(D.querySelector('.view.active')||D.querySelector('.page.active'))?.id||axis818Route||'todayView'}
function axis818RouteGuard(){if(axis818RouteLock)return;axis818RouteLock=true;try{const active=axis818CurrentRoute();if(active)axis818Route=active;const today=axis818Route==='todayView';for(const v of $$('.view,.page')){const on=v.id===axis818Route;v.toggleAttribute('inert',!on);v.setAttribute('aria-hidden',on?'false':'true')}for(const b of $$('[data-view]'))b.classList.toggle('active',b.dataset.view===axis818Route);for(const id of ['dock','v87Now','v82ActiveRail']){const x=$('#'+id);if(x&&!today){x.classList.remove('show');x.setAttribute('aria-hidden','true')}else if(x&&today)x.removeAttribute('aria-hidden')}D.body.classList.toggle('axis818-route-away',!today)}finally{axis818RouteLock=false}}
D.addEventListener('click',e=>{const b=e.target.closest('[data-view]');if(b?.dataset.view){axis818Route=b.dataset.view;setTimeout(axis818RouteGuard,0)}},true);
D.addEventListener('visibilitychange',()=>{if(!D.hidden){setTimeout(axis818RouteGuard,0);setTimeout(axis818RouteGuard,120);setTimeout(axis818RouteGuard,360)}});
window.addEventListener('pageshow',()=>{setTimeout(axis818RouteGuard,0);setTimeout(axis818RouteGuard,140);setTimeout(axis818RouteGuard,420)});
window.addEventListener('focus',()=>setTimeout(axis818RouteGuard,40));
try{new MutationObserver(()=>{const r=axis818CurrentRoute();if(r!==axis818Route)axis818Route=r;queueMicrotask(axis818RouteGuard)}).observe(D.querySelector('.app')||D.body,{subtree:true,attributes:true,attributeFilter:['class']})}catch{}

/* Capture preferences grow with the actual Photo / Scan / Video / facing capabilities. */
function axis818CapturePrefs(){state.prefs.captureDefaultMode=state.prefs.captureDefaultMode||'last';state.prefs.captureDefaultFacing=state.prefs.captureDefaultFacing||'last';state.prefs.captureLastMode=state.prefs.captureLastMode||'photo';state.prefs.captureLastFacing=state.prefs.captureLastFacing||'environment';return state.prefs}
function axis818DesiredMode(){const p=axis818CapturePrefs();return p.captureDefaultMode==='last'?(p.captureLastMode||'photo'):p.captureDefaultMode}
function axis818DesiredFacing(){const p=axis818CapturePrefs();return p.captureDefaultFacing==='last'?(p.captureLastFacing||'environment'):p.captureDefaultFacing}
function axis818RenderCapturePrefs(){const sec=$('#scanSeconds');if(!sec)return;let host=$('#axis818CapturePrefs');if(!host){host=D.createElement('div');host.id='axis818CapturePrefs';host.className='axis818CapturePrefs';sec.closest('.v82Setting,.settingPlain,.settingLink')?.insertAdjacentElement('beforebegin',host)}if(!host)return;const p=axis818CapturePrefs(),row=(title,key,vals)=>'<div class="axis818PrefRow"><span>'+title+'</span><div>'+vals.map(v=>'<button type="button" data-axis818-pref="'+key+'" data-value="'+v[0]+'" class="'+(p[key]===v[0]?'active':'')+'">'+v[1]+'</button>').join('')+'</div></div>';host.innerHTML=row('默认拍摄入口','captureDefaultMode',[['last','上次'],['photo','照片'],['scan','扫描'],['video','视频']])+row('默认镜头','captureDefaultFacing',[['last','上次'],['environment','后置'],['user','前置']]);}
D.addEventListener('click',e=>{const b=e.target.closest('[data-axis818-pref]');if(!b)return;e.preventDefault();e.stopPropagation();const k=b.dataset.axis818Pref,v=b.dataset.value;if(k==='captureDefaultMode'&&['last','photo','scan','video'].includes(v))state.prefs[k]=v;if(k==='captureDefaultFacing'&&['last','environment','user'].includes(v))state.prefs[k]=v;save();axis818RenderCapturePrefs()},true);
D.addEventListener('click',e=>{const b=e.target.closest('#v816CaptureMode [data-v816-mode]');if(b){axis818CapturePrefs().captureLastMode=b.dataset.v816Mode;save()}},true);
D.addEventListener('click',e=>{if(e.target.closest('#settingsBtn'))setTimeout(axis818RenderCapturePrefs,150)},true);

/* Export and Encounter deletion share the canonical store and clean-sidecar refs. */
async function axis818ShareFiles(files){if(!files.length)return;if(navigator.canShare?.({files})&&navigator.share){await navigator.share({files});return}for(const f of files){const a=D.createElement('a');a.href=URL.createObjectURL(f);a.download=f.name;D.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1200);await new Promise(r=>setTimeout(r,120))}}
async function axis818Files(e,kind='all',source=false){const out=[],sourceBridge=window.__AXIS_MEDIA_SOURCE__,get=async ref=>source&&sourceBridge?.readOnly?await sourceBridge.get(ref):await getMedia(ref);if(kind!=='video')for(let i=0;i<(e.frameRefs||[]).length;i++){const ref=source?(e.sourceFrameRefs?.[i]||e.frameRefs[i]):e.frameRefs[i],b=await get(ref);if(b)out.push(new File([b],'AXIS-'+e.name+'-'+(i+1)+'.jpg',{type:b.type||'image/jpeg'}))}if(kind!=='photo'&&e.clipRef){const ref=source?(e.sourceClipRef||e.clipRef):e.clipRef,b=await get(ref);if(b)out.push(new File([b],'AXIS-'+e.name+'.'+((b.type||'video/mp4').includes('webm')?'webm':'mp4'),{type:b.type||'video/mp4'}))}return out}
async function axis818DeleteEvent(id){const all=[...(state.active?[state.active]:[]),...(state.sessions||[])],s=all.find(x=>ev(x).some(e=>e.id===id));if(!s)return false;const e=ev(s).find(x=>x.id===id);let m={};try{m=JSON.parse(localStorage.getItem('axis_v8_meta')||'{}')||{}}catch{};const a=m.events?.[id]?.activity;if(s===state.active&&a?.status!=='finished'){toast('进行中的项目请先完成');return false}if(!confirm('删除这条记录？照片、视频和对应数据会一起删除。'))return false;for(const r of [...(e.frameRefs||[]),...(e.sourceFrameRefs||[]),e.clipRef,e.sourceClipRef].filter(Boolean))try{await deleteMedia(r)}catch{}s.events=(s.events||[]).filter(x=>x.id!==id);if(m.events)delete m.events[id];localStorage.setItem('axis_v8_meta',JSON.stringify(m));if(s!==state.active&&!s.events.length)state.sessions=state.sessions.filter(x=>x.id!==s.id);save();closeSheet('detailSheet');render();toast('已删除');return true}
window.__AXIS_818_MEDIA__={version:'8.18',owner:'app.js',shareFiles:axis818ShareFiles,files:axis818Files,deleteEvent:axis818DeleteEvent};

/* Derived Evolution Object shelf: no duplicate persistence, no new database. */
function axis818Objects(){const map=new Map();for(const e of allEvents()){const key=e.equipmentId||('name:'+e.name),o=map.get(key)||{id:key,name:e.name,count:0,last:0,first:Infinity,evidence:0,schema:axis818SchemaForEvent(e)};o.count++;o.last=Math.max(o.last,Number(e.time)||0);o.first=Math.min(o.first,Number(e.time)||Infinity);o.evidence+=(e.frameRefs?.length||0)+(e.clipRef?1:0);map.set(key,o)}return[...map.values()].sort((a,b)=>b.last-a.last)}
function axis818RenderShelf(){const target=$('#historyView,#recordsView,#trendView,#trendsView');if(!target)return;let host=$('#axis818ObjectShelf');if(!host){host=D.createElement('section');host.id='axis818ObjectShelf';host.className='axis818ObjectShelf';target.prepend(host)}const xs=axis818Objects();host.innerHTML='<div class="axis818ShelfHead"><span>Evolution Library</span><b>'+xs.length+' 个对象</b></div><div class="axis818ShelfRail">'+xs.slice(0,12).map(o=>'<button type="button" data-axis818-object="'+esc(o.id)+'"><b>'+esc(o.name)+'</b><span>'+o.count+' 次 · '+o.evidence+' 份影像</span></button>').join('')+'</div>'}
window.__AXIS_EVOLUTION_LIBRARY__={version:'8.18',owner:'derived-read-only',objects:axis818Objects,render:axis818RenderShelf,persistence:false};
setTimeout(()=>{axis818RouteGuard();axis818RenderCapturePrefs();axis818RenderShelf()},260);
`;
 s=s.slice(0,end)+block+'\n'+s.slice(end);

 /* Canonical event save snapshots the object's actual metric schema. */
 s=once(s,"if(eq.type==='strength'){e.weight=nval('weight',0,1000,0);e.reps=choiceVal('reps',10);e.sets=choiceVal('sets',3)}else{e.duration=nval('duration',1,600,15);e.intensity=choiceVal('intensity',5)}try{","if(eq.type==='strength'){e.weight=nval('weight',0,1000,0);e.reps=choiceVal('reps',10);e.sets=choiceVal('sets',3)}else{e.duration=nval('duration',1,600,15);e.intensity=choiceVal('intensity',5)}axis818CaptureEvent(e,eq);try{",'metric schema snapshot at event save');

 /* Replace the single-file event exporter with complete photo/video/all behavior and canonical metric rows. */
 s=regexOnce(s,/async function openEvent\(id\)\{[\s\S]*?\}\nfunction openSession/,
`async function openEvent(id){const e=allEvents().find(x=>x.id===id);if(!e)return;const eq=eventEq(e);setText('#detailTitle',e.name);let media='';if(e.clipRef){const v=await mediaUrl(e.clipRef),poster=e.frameRefs?.[0]?await mediaUrl(e.frameRefs[0]):'';if(v)media+='<div class="videoCard"><div class="mediaTop"><b>现场视频</b><span>'+(e.videoWatermarked?'带水印':'原片')+'</span></div><video class="detailVideo" src="'+v+'" '+(poster?'poster="'+poster+'"':'')+' controls playsinline muted></video></div>'}if(e.frameRefs?.length){const imgs=[];for(const r of e.frameRefs){const u=await mediaUrl(r);if(u)imgs.push('<img src="'+u+'" alt="现场照片">')}media+='<div class="mediaTop photoTop"><b>现场照片</b><span>'+imgs.length+'张</span></div><div class="detailFilm">'+imgs.join('')+'</div>'}const rows=[['主要锻炼',(e.muscles||eq.muscles||[]).join(' · ')||'—'],...axis818EventRows(e),['记录时间',dlabel(e.time)+' '+tlabel(e.time)]];const buttons=[];if(e.frameRefs?.length)buttons.push('<button id="axis818SavePhotos">保存照片</button>');if(e.clipRef)buttons.push('<button id="axis818SaveVideo">保存视频</button>');if(e.frameRefs?.length&&e.clipRef)buttons.push('<button id="axis818SaveAll">保存全部</button>');buttons.push('<button class="axis818DeleteEvent" id="axis818DeleteEvent">删除记录</button>');$('#detail').innerHTML=media+'<div class="detailList">'+rows.map(r=>'<div class="detailRow"><span>'+esc(r[0])+'</span><span>'+esc(r[1])+'</span></div>').join('')+'</div><div class="detailActions axis818DetailActions">'+buttons.join('')+'<button id="makeSessionReport">训练报告</button></div>';openSheet('detailSheet');const share=async kind=>{try{await axis818ShareFiles(await axis818Files(e,kind,false))}catch(x){if(x?.name!=='AbortError'){console.warn(x);toast('保存失败')}}};$('#axis818SavePhotos')&&($('#axis818SavePhotos').onclick=()=>share('photo'));$('#axis818SaveVideo')&&($('#axis818SaveVideo').onclick=()=>share('video'));$('#axis818SaveAll')&&($('#axis818SaveAll').onclick=()=>share('all'));$('#axis818DeleteEvent')&&($('#axis818DeleteEvent').onclick=()=>axis818DeleteEvent(e.id));if($('#makeSessionReport'))$('#makeSessionReport').onclick=()=>{const s=(state.active?[state.active]:[]).concat(state.sessions).find(s=>ev(s).some(x=>x.id===e.id));if(s){reportRange='session:'+s.id;renderReport();openSheet('reportSheet')}}}
function openSession`,'event detail/export/delete convergence');

 syntax(s,FILE);write(FILE,s);
}

/* --------------------------------------------------------------------------
 * 2. v874 stays the ONE visible custom-object editor. Add metric schema there.
 * ----------------------------------------------------------------------- */
{
 const FILE='v874-professional.js';let s=read(FILE),end=s.lastIndexOf('})();');if(end<0)fail('v874 IIFE end missing');
 const block=String.raw`
/* AXIS 8.18 — metric schema editor remains inside the canonical v874 editor owner. */
const AXIS818_METRIC_CHOICES=[['weight','重量','kg','number'],['reps','次数','次','number'],['sets','组数','组','number'],['duration','时间','分钟','duration'],['intensity','强度','','number'],['distance','距离','km','number'],['resistance','阻力 / 档位','','number'],['pace','速度 / 配速','','text'],['hold','保持时间','秒','duration']];
let axis818MetricDraft=[];
function axis818MetricDefault(type){return(type==='cardio'?[['duration','时间','分钟','duration'],['intensity','强度','','number']]:[['weight','重量','kg','number'],['reps','次数','次','number'],['sets','组数','组','number']]).map(x=>({key:x[0],label:x[1],unit:x[2],type:x[3],step:1}))}
function axis818MetricLoad(){const c=readCore(),eq=editId?(c.profile?.customEq||[]).find(x=>x.id===editId):null;axis818MetricDraft=(Array.isArray(eq?.metricSchema)&&eq.metricSchema.length?eq.metricSchema:axis818MetricDefault(eq?.type||($('#customType .active')?.dataset.type)||'strength')).map(x=>({...x}));axis818MetricRender()}
function axis818MetricEnsure(){let host=$('#axis818MetricEditor');if(host)return host;const save=$('#saveCustomEq');if(!save)return null;host=D.createElement('section');host.id='axis818MetricEditor';host.className='axis818MetricEditor';save.parentNode.insertBefore(host,save);return host}
function axis818MetricRender(){const host=axis818MetricEnsure();if(!host)return;const has=k=>axis818MetricDraft.some(x=>x.key===k);host.innerHTML='<div class="axis818MetricEditorHead"><span>记录属性</span><b>选择这个项目真正需要记录的内容</b></div><div class="axis818MetricChoices">'+AXIS818_METRIC_CHOICES.map(x=>'<button type="button" data-axis818-metric-choice="'+x[0]+'" class="'+(has(x[0])?'active':'')+'">'+x[1]+'</button>').join('')+'<button type="button" data-axis818-metric-custom="1">＋ 自定义</button></div><div class="axis818MetricSelected">'+axis818MetricDraft.map((x,i)=>'<button type="button" data-axis818-metric-remove="'+i+'"><span>'+esc(x.label)+(x.unit?' · '+esc(x.unit):'')+'</span><i>×</i></button>').join('')+'</div>'}
function axis818MetricToggle(k){const i=axis818MetricDraft.findIndex(x=>x.key===k);if(i>=0)axis818MetricDraft.splice(i,1);else{const x=AXIS818_METRIC_CHOICES.find(v=>v[0]===k);if(x)axis818MetricDraft.push({key:x[0],label:x[1],unit:x[2],type:x[3],step:1})}if(!axis818MetricDraft.length)axis818MetricDraft.push({key:'duration',label:'时间',unit:'分钟',type:'duration',step:1});axis818MetricRender()}
function axis818MetricCustom(){const label=prompt('属性名称，例如「坡度」或「RPE」','');if(!label?.trim())return;const unit=prompt('单位（可留空）','')||'';axis818MetricDraft.push({key:'custom_'+Date.now().toString(36),label:label.trim(),unit:unit.trim(),type:'number',step:1,custom:true});axis818MetricRender()}
function axis818MetricPersist(){const c=readCore(),name=$('#customName')?.value.trim()||'';setTimeout(()=>{const cc=readCore(),eq=(cc.profile?.customEq||[]).find(x=>x.id===editId)||(cc.profile?.customEq||[]).filter(x=>x.name===name).at(-1);if(!eq)return;eq.metricSchema=axis818MetricDraft.map(x=>({...x}));eq.metricSchemaVersion='8.18';writeCore(cc);window.dispatchEvent(new CustomEvent('axis:object-schema-changed',{detail:{id:eq.id,schema:eq.metricSchema}}))},90)}
D.addEventListener('click',e=>{const b=e.target.closest('[data-axis818-metric-choice]');if(b){e.preventDefault();e.stopPropagation();axis818MetricToggle(b.dataset.axis818MetricChoice);return}const rm=e.target.closest('[data-axis818-metric-remove]');if(rm){e.preventDefault();e.stopPropagation();axis818MetricDraft.splice(Number(rm.dataset.axis818MetricRemove),1);if(!axis818MetricDraft.length)axis818MetricDraft.push({key:'duration',label:'时间',unit:'分钟',type:'duration',step:1});axis818MetricRender();return}if(e.target.closest('[data-axis818-metric-custom]')){e.preventDefault();e.stopPropagation();axis818MetricCustom();return}if(e.target.closest('#addCustomEq,#v8New,#newCustomEq,[data-edit-eq]'))setTimeout(axis818MetricLoad,80);if(e.target.closest('#saveCustomEq'))axis818MetricPersist()},true);
window.__AXIS_CUSTOM_EDITOR__.metricSchema=()=>axis818MetricDraft.map(x=>({...x}));
`;
 s=s.slice(0,end)+block+'\n'+s.slice(end);syntax(s,FILE);write(FILE,s);
}

/* --------------------------------------------------------------------------
 * 3. v87 direct active-control owner gains optional Focus Layer only.
 *    It delegates completion back to #v87Primary; it never owns sound or completion.
 * ----------------------------------------------------------------------- */
{
 const FILE='v87-runtime.js';let s=read(FILE),end=s.lastIndexOf('})();');if(end<0)fail('v87 IIFE end missing');
 const block=String.raw`
/* AXIS 8.18 — optional Active Focus Layer. Existing v87 action owner stays canonical. */
let axis818FocusHold=0,axis818FocusStart=0;
function axis818FocusEnsure(){if($('#axis818Focus'))return;D.body.insertAdjacentHTML('beforeend','<div class="axis818Focus" id="axis818Focus" aria-hidden="true"><button class="axis818FocusClose" id="axis818FocusClose" aria-label="关闭">×</button><div class="axis818FocusCore"><span id="axis818FocusState"></span><h2 id="axis818FocusName"></h2><b id="axis818FocusClock">00:00</b><small id="axis818FocusMeta"></small><div class="axis818FocusAxis"><i id="axis818FocusAxisFill"></i></div><button type="button" class="axis818FocusTap" id="axis818FocusTap">完成一组</button><button type="button" class="axis818FocusHold" id="axis818FocusHold"><i></i><span>按住完成一组</span></button></div></div>');$('#axis818FocusClose').onclick=axis818FocusClose;$('#axis818FocusTap').onclick=()=>$('#v87Primary:not(:disabled)')?.click();const h=$('#axis818FocusHold');h.addEventListener('pointerdown',e=>{if($('#v87Primary')?.disabled)return;axis818FocusStart=performance.now();h.setPointerCapture?.(e.pointerId);const step=t=>{const p=Math.min(1,(t-axis818FocusStart)/620);h.style.setProperty('--hold',p);if(p<1)axis818FocusHold=requestAnimationFrame(step);else{cancelAnimationFrame(axis818FocusHold);axis818FocusHold=0;h.style.setProperty('--hold',0);$('#v87Primary:not(:disabled)')?.click();try{navigator.vibrate?.(14)}catch{}}};axis818FocusHold=requestAnimationFrame(step)});for(const ev of ['pointerup','pointercancel','pointerleave'])h.addEventListener(ev,()=>{if(axis818FocusHold)cancelAnimationFrame(axis818FocusHold);axis818FocusHold=0;h.style.setProperty('--hold',0)})}
function axis818FocusOpen(){if(!$('#v87Now')?.classList.contains('show'))return;axis818FocusEnsure();$('#axis818Focus').classList.add('show');$('#axis818Focus').setAttribute('aria-hidden','false');D.body.classList.add('axis818-focus');axis818FocusSync()}
function axis818FocusClose(){if(axis818FocusHold)cancelAnimationFrame(axis818FocusHold);axis818FocusHold=0;$('#axis818Focus')?.classList.remove('show');$('#axis818Focus')?.setAttribute('aria-hidden','true');D.body.classList.remove('axis818-focus')}
function axis818FocusSync(){const f=$('#axis818Focus');if(!f?.classList.contains('show'))return;const t=activePair()||pausedPairs()[0];if(!t){axis818FocusClose();return}const {e,a}=t,m=readMeta(),actual=elapsed(a),est=Math.max(60000,Number(a.estimateMs)||actual||60000),remaining=Math.max(0,est-actual),rest=a.restStartedAt&&a.status==='active'?now()-a.restStartedAt:0,total=planned(e,m),done=Math.max(0,Number(a.completedSets)||0);$('#axis818FocusState').textContent=a.status==='paused'?'已暂停':rest?'间歇':'进行中';$('#axis818FocusName').textContent=e.name||'项目';$('#axis818FocusClock').textContent=rest?clock(rest):clock(remaining);$('#axis818FocusMeta').textContent=rest?'本组已完成 · 休息计时':('实际 '+clock(actual)+(e.kind==='strength'?' · '+done+'/'+total+'组':''));$('#axis818FocusAxisFill').style.width=Math.min(100,actual/est*100)+'%';const can=e.kind==='strength'&&a.status==='active'&&!$('#v87Primary')?.disabled;$('#axis818FocusTap').hidden=!can;$('#axis818FocusHold').hidden=!can}
D.addEventListener('click',e=>{if(e.target.closest('#v87Main,#v87Meta,#v87Name')&&!e.target.closest('button'))axis818FocusOpen()},true);
D.addEventListener('visibilitychange',()=>{if(D.hidden)axis818FocusClose()});
window.__AXIS_818_FOCUS__={version:'8.18',owner:'presentation-only',open:axis818FocusOpen,close:axis818FocusClose,completionOwner:'v87-direct-884',automatic:false};
`;
 s=s.slice(0,end)+block+'\n'+s.slice(end);
 s=s.replace("host.classList.add('show');D.body.classList.add('v87-now')}","host.classList.add('show');D.body.classList.add('v87-now');axis818FocusSync()}");
 syntax(s,FILE);write(FILE,s);
}

/* --------------------------------------------------------------------------
 * 4. Camera/video: one logical MediaRecorder records a stable canvas compositor.
 *    Switching the physical camera beneath #cameraVideo no longer tears recording.
 * ----------------------------------------------------------------------- */
{
 const FILE='app.js';let s=read(FILE);
 const re=/async function capture816StartVideo\(\)\{[\s\S]*?\}\nfunction capture816StopVideo/;
 const fn=`let axis818RecordCanvas=null,axis818RecordCtx=null,axis818RecordStream=null,axis818RecordPump=0,axis818RecordAlive=false;
function axis818StopRecordPump(){axis818RecordAlive=false;if(axis818RecordPump){cancelAnimationFrame(axis818RecordPump);clearInterval(axis818RecordPump)}axis818RecordPump=0;try{axis818RecordStream?.getTracks().forEach(t=>t.stop())}catch{}axis818RecordStream=null;axis818RecordCanvas=null;axis818RecordCtx=null}
function axis818StartRecordPump(){const v=$('#cameraVideo');if(!v?.videoWidth)return null;const cv=D.createElement('canvas');cv.width=v.videoWidth;cv.height=v.videoHeight;const c=cv.getContext('2d',{alpha:false,desynchronized:true});c.imageSmoothingEnabled=true;c.imageSmoothingQuality='high';axis818RecordCanvas=cv;axis818RecordCtx=c;axis818RecordAlive=true;const draw=()=>{if(!axis818RecordAlive)return;try{c.drawImage(v,0,0,cv.width,cv.height)}catch{};if(v.requestVideoFrameCallback)v.requestVideoFrameCallback(draw);else axis818RecordPump=requestAnimationFrame(draw)};draw();axis818RecordStream=cv.captureStream(30);return axis818RecordStream}
async function capture816StartVideo(){if(capture816Recorder&&capture816Recorder.state==='recording')return true;if(!state.stream||!window.MediaRecorder){toast('当前设备暂不支持视频拍摄');return false}capture816UiMode='video';captureMode='video';state.forceClip=true;capture816Chunks=[];capture816Discard=false;capture816StartedAt=Date.now();const stable=axis818StartRecordPump();if(!stable){toast('视频启动失败');return false}var rec=null,mt=mediaMime(),base=mt?{mimeType:mt}:{};try{rec=new MediaRecorder(stable,Object.assign({},base,{videoBitsPerSecond:6000000}))}catch(e){try{rec=new MediaRecorder(stable,base)}catch(x){axis818StopRecordPump();console.warn(x);toast('当前设备暂不支持视频拍摄');return false}}capture816Recorder=rec;rec.ondataavailable=e=>{if(e.data&&e.data.size)capture816Chunks.push(e.data)};rec.onstop=function(){clearTimeout(capture816StopTimer);capture816StopTimer=null;const discard=capture816Discard,dur=Math.min(CAPTURE816_VIDEO_MAX_MS,Math.max(0,Date.now()-capture816StartedAt)),chunks=capture816Chunks.slice(),type=rec.mimeType||(chunks[0]&&chunks[0].type)||'video/webm';capture816Recorder=null;capture816Chunks=[];capture816Discard=false;axis818StopRecordPump();if(!discard&&chunks.length){const blob=new Blob(chunks,{type});if(state.clip?.url)try{URL.revokeObjectURL(state.clip.url)}catch{}state.clip={blob,url:URL.createObjectURL(blob),type:blob.type,durationMs:dur,compositor:'axis818-canvas-30fps'}}capture816StopTick();capture816StopWaiters.splice(0).forEach(r=>r());capture816Notify()};try{rec.start(500)}catch(e){capture816Recorder=null;axis818StopRecordPump();console.warn(e);toast('视频启动失败');return false}capture816StopTimer=setTimeout(()=>{toast('已录满 60 秒');capture816StopVideo(false)},CAPTURE816_VIDEO_MAX_MS);capture816StartTick();capture816Notify();return true}
function capture816StopVideo`;
 s=regexOnce(s,re,fn,'stable canvas video recorder');
 s=s.replace("function capture8171PaintFacing(){const b=$('#v8171CameraFlip');if(!b)return;b.textContent=capture8171Facing==='user'?'前置':'后置';b.setAttribute('aria-label','切换摄像头，当前'+b.textContent);b.disabled=!!capture8171FlipBusy||!!(capture816Recorder&&capture816Recorder.state==='recording')}","function capture8171PaintFacing(){const b=$('#v8171CameraFlip');if(!b)return;b.textContent=capture8171Facing==='user'?'前置':'后置';b.setAttribute('aria-label','切换摄像头，当前'+b.textContent);b.disabled=!!capture8171FlipBusy}");
 s=s.replace("async function capture8171FlipCamera(){if(capture8171FlipBusy)return false;if(capture816Recorder&&capture816Recorder.state==='recording'){toast('录制结束后可切换镜头');capture8171PaintFacing();return false}capture8171FlipBusy=true;","async function capture8171FlipCamera(){if(capture8171FlipBusy)return false;capture8171FlipBusy=true;");
 s=s.replace("capture8171Facing=before==='user'?'environment':'user';try{await startCamera();return true}","capture8171Facing=before==='user'?'environment':'user';axis818CapturePrefs().captureLastFacing=capture8171Facing;save();try{await startCamera();return true}");
 syntax(s,FILE);write(FILE,s);
}

/* --------------------------------------------------------------------------
 * 5. v8710 remains the ONE watermark design owner. Re-enable the requested
 *    center AXIS brand and expose that ownership for the 8.18 video compositor.
 * ----------------------------------------------------------------------- */
{
 const FILE='v8710-watermark.js';let s=read(FILE),end=s.lastIndexOf('})();');if(end<0)fail('watermark IIFE end missing');
 s=once(s,'/* AXIS 8.15.1 center brand retired */',"c.fillText('AXIS',W/2,H*.48);",'8.18 center AXIS brand');
 const marker=String.raw`
try{window.__AXIS_818_WATERMARK__={version:'8.18',owner:'v8710-watermark',centerBrand:true,photoSource:'clean-sidecar-first',videoSource:'clean-sidecar-first',opacityOwner:'brand-opacity',legacyCompositors:false}}catch{}
`;
 s=s.slice(0,end)+marker+s.slice(end);syntax(s,FILE);write(FILE,s);
}

/* Styles are additive and scoped; existing accepted layout remains the baseline. */
{
 const FILE='styles.css';let c=read(FILE);if(c.includes('AXIS 8.18 Object + Focus foundation'))fail('8.18 CSS duplicated');
 c+=`\n\n/* AXIS 8.18 Object + Focus foundation */\n.axis818LegacyMetricHidden{display:none!important}.axis818MetricRecorder{display:none;margin:12px 0 4px;border-top:1px solid var(--line2)}.axis818MetricRecorder.show{display:block}.axis818MetricHead{height:52px;display:flex;align-items:center;justify-content:space-between;gap:12px}.axis818MetricHead span{font-size:12px;color:var(--muted)}.axis818MetricHead b{max-width:60%;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.axis818MetricField{min-height:64px;display:grid;grid-template-columns:minmax(0,1fr) minmax(120px,42%);align-items:center;gap:12px;border-bottom:1px solid var(--line2)}.axis818MetricField>span{font-size:13px;color:var(--muted)}.axis818MetricField>div{height:44px;display:flex;align-items:center;justify-content:flex-end;gap:6px;border-radius:13px;background:var(--s2);padding:0 12px}.axis818MetricField input{min-width:0;width:100%;border:0;background:transparent;color:var(--text);font-size:18px;text-align:right;outline:0}.axis818MetricField small{font-size:10px;color:var(--dim);white-space:nowrap}.axis818MetricEditor{padding:15px 0;border-top:1px solid var(--line)}.axis818MetricEditorHead{display:flex;align-items:baseline;justify-content:space-between;gap:12px}.axis818MetricEditorHead span{font-size:13px;color:var(--text)}.axis818MetricEditorHead b{font-size:9.5px;color:var(--dim);font-weight:560;text-align:right}.axis818MetricChoices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px;margin-top:12px}.axis818MetricChoices button{min-height:40px;border-radius:12px;background:var(--s2);color:var(--muted);font-size:11px}.axis818MetricChoices button.active{background:rgba(115,124,255,.15);color:#c4c8ff;box-shadow:inset 0 0 0 1px rgba(115,124,255,.22)}.axis818MetricSelected{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.axis818MetricSelected button{height:30px;display:flex;align-items:center;gap:7px;padding:0 9px;border-radius:10px;background:#11151b;color:#aeb4bf;font-size:10px}.axis818MetricSelected i{font-style:normal;color:#747d89}.axis818CapturePrefs{padding:4px 0 10px;border-bottom:1px solid var(--line2)}.axis818PrefRow{min-height:62px;display:grid;grid-template-columns:112px minmax(0,1fr);align-items:center;gap:12px}.axis818PrefRow>span{font-size:13px;color:var(--muted)}.axis818PrefRow>div{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(0,1fr);gap:3px;padding:3px;border-radius:13px;background:var(--s2)}.axis818PrefRow button{height:36px;border-radius:10px;color:var(--muted);font-size:10.5px}.axis818PrefRow button.active{background:#2a2e38;color:var(--text)}body.axis818-route-away #dock,body.axis818-route-away #v87Now,body.axis818-route-away #v82ActiveRail{display:none!important;pointer-events:none!important}.axis818Focus{position:fixed;inset:0;z-index:340;display:none;background:radial-gradient(circle at 50% 28%,rgba(115,124,255,.13),transparent 32%),#07090d;color:var(--text);padding:calc(22px + env(safe-area-inset-top)) 24px calc(26px + env(safe-area-inset-bottom));box-sizing:border-box}.axis818Focus.show{display:flex;align-items:center;justify-content:center}.axis818FocusClose{position:absolute;right:20px;top:calc(16px + env(safe-area-inset-top));width:44px;height:44px;border-radius:14px;background:rgba(255,255,255,.055);color:#aeb5c0;font-size:22px}.axis818FocusCore{width:min(100%,520px);text-align:center}.axis818FocusCore>span{font-size:12px;color:#aeb3ff;letter-spacing:.08em}.axis818FocusCore h2{margin:12px 0 0;font-size:22px;font-weight:680}.axis818FocusCore>b{display:block;margin-top:34px;font-size:clamp(64px,19vw,108px);line-height:.9;letter-spacing:-.055em;font-variant-numeric:tabular-nums}.axis818FocusCore>small{display:block;margin-top:18px;color:#858e9b;font-size:12px}.axis818FocusAxis{height:3px;margin:34px auto 0;border-radius:99px;background:#171b23;overflow:hidden}.axis818FocusAxis i{display:block;height:100%;width:0;background:#737cff;transition:width .25s ease}.axis818FocusTap,.axis818FocusHold{width:100%;height:58px;margin-top:24px;border-radius:18px;font-size:14px;font-weight:680}.axis818FocusTap{background:#f0efeb;color:#0b0d10}.axis818FocusHold{position:relative;overflow:hidden;margin-top:10px;background:#151922;color:#b9c0cb}.axis818FocusHold i{position:absolute;inset:0 auto 0 0;width:calc(var(--hold,0)*100%);background:rgba(115,124,255,.20)}.axis818FocusHold span{position:relative}.axis818DetailActions{display:grid!important;grid-template-columns:repeat(2,minmax(0,1fr))!important;gap:10px!important}.axis818DetailActions button{min-width:0!important}.axis818DeleteEvent{color:#ff8b83!important}.axis818ObjectShelf{margin:0 0 22px;padding:16px 0;border-bottom:1px solid var(--line2)}.axis818ShelfHead{display:flex;align-items:center;justify-content:space-between}.axis818ShelfHead span{font-size:12px;color:#aeb3ff}.axis818ShelfHead b{font-size:10px;color:var(--dim);font-weight:580}.axis818ShelfRail{display:flex;gap:8px;overflow-x:auto;margin-top:11px;scrollbar-width:none}.axis818ShelfRail button{flex:0 0 148px;min-height:70px;padding:12px;border-radius:16px;background:#11151b;text-align:left}.axis818ShelfRail b{display:block;font-size:12.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.axis818ShelfRail span{display:block;margin-top:6px;color:#747d89;font-size:9.5px}@media(max-width:390px){.axis818MetricChoices{grid-template-columns:repeat(2,minmax(0,1fr))}.axis818PrefRow{grid-template-columns:94px minmax(0,1fr)}.axis818Focus{padding-left:20px;padding-right:20px}}\n`;
 write(FILE,c);
}

/* Final source-level invariants before the canonical bundle is built. */
{
 const app=read('app.js'),v874=read('v874-professional.js'),v87=read('v87-runtime.js'),wm=read('v8710-watermark.js');
 for(const x of ['__AXIS_OBJECT_TRUTH__','metricSchemaSnapshot','axis818RouteGuard','captureDefaultMode','__AXIS_818_MEDIA__','__AXIS_EVOLUTION_LIBRARY__'])if(!app.includes(x))fail(`app invariant missing ${x}`);
 for(const x of ['axis818MetricDraft','metricSchemaVersion','__AXIS_CUSTOM_EDITOR__.metricSchema'])if(!v874.includes(x))fail(`custom metric invariant missing ${x}`);
 for(const x of ['__AXIS_818_FOCUS__','axis818FocusOpen','completionOwner:\'v87-direct-884\''])if(!v87.includes(x))fail(`focus invariant missing ${x}`);
 if(!wm.includes("c.fillText('AXIS',W/2,H*.48)"))fail('center AXIS brand not restored');
 if(!app.includes("compositor:'axis818-canvas-30fps'"))fail('stable video compositor missing');
}
console.log('[AXIS 8.18 foundation] PASS · Object Truth · Route Truth · Capture prefs · batch export/delete · Focus · stable camera compositor · current AXIS brand');
